namespace eval ::euidl::uidlmenu::uidlmenuItems {
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_My_steel) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "parameter of colunm"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_My_steel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_My_silicon) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "parameter of plan"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_My_silicon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Steel_column) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Model_Support"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Steel_column__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Silicon_plate) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Model_Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Silicon_plate__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Mesh_steel) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Mesh_Support"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Mesh_steel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Mesh_silicon) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Mesh_Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Mesh_silicon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Model_DDof) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Model_DDof"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Model_DDof__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Model_SSolve) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Model_SSolve"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Model_SSolve__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_X_stress) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "X-Structure-stress"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_X_stress__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Y_stress) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Y-Structure-stress"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Y_stress__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Z_stress) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Z-Structure-stress"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Z_stress__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FN-stress) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "FN-stress"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FN-stress__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_X-Structure-displacement) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "X-Structure-displacement"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_X-Structure-displacement__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Y-Structure-displacement) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Y-Structure-displacement"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Y-Structure-displacement__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Z-Structure-displacement) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Z-Structure-displacement"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Z-Structure-displacement__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Total-Structure-displacement) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Total-Structure-displacement"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Total-Structure-displacement__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OMEGA_dele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OMEGA_dele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OPNCONTROL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (NLCNTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Open Control"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OPNCONTROL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTPR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solu Printout"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTPR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTPR_ADD1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTPR_ADD1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTPR_ADD2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTPR_ADD2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTPR_cw) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Printout Ctrls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTPR_cw__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "DB/Results File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES_31) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES_31__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES_311) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES_311__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES_32) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES_32__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES_321) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES_321__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES_322) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES_322__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES_323) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES_323__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES_ADD1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES_ADD1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES_ADD2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES_ADD2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES_ADD3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES_ADD3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES_ADD4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES_ADD4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUTRES_cw) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "DB/Results File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUTRES_cw__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OUT_stat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Show Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OUT_stat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OceanRead) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Import Ocean"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OceanRead__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OptFil) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list opt]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Opt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OptFil__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OutOpts2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OutOpts2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OutOpts3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OutOpts3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OutOpts_p1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Options for Outp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OutOpts_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_P26VAR_ADD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add Variable"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_P26VAR_ADD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_P26VAR_ADD2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add Variable"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_P26VAR_ADD2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_P26VAR_EDIT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Edit Variable"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_P26VAR_EDIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_P26_E_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element for Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_P26_E_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_P26_N_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Node for Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_P26_N_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAGET_AR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Path in array"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAGET_AR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAGET_C0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAGET_C0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAGET_C1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAGET_C1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAGET_LA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAGET_LA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAGET_PO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAGET_PO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAGET_RR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAGET_RR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAGET_TA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAGET_TA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAPUT_AR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Path from array"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAPUT_AR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAPUT_C0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Path from array"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAPUT_C0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAPUT_C1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Path from array"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAPUT_C1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAPUT_DD) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAPUT_DD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAPUT_EE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAPUT_EE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAPUT_RQ) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAPUT_RQ__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PAPUT_RR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PAPUT_RR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PARRES) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Restore Parameters"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PARRES__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PARSAV) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Save Parameters"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PARSAV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHDELALL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "All Paths"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHDELALL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHDELNAM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Name"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHDELNAM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHEDIT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHEDIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHOPTS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Path Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHOPTS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Paths"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHRECALL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Recall Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHRECALL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHRESU) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Paths from file"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHRESU__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHSAVALL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "All Paths"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHSAVALL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHSAVE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Paths in file"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHSAVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHSAVNAM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Paths by name"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHSAVNAM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHSTAT_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Defined Paths"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHSTAT_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PATHSTAT_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Current Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PATHSTAT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PBAKQTY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PBAKQTY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PBAK_COLR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picture Background Shading"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PBAK_COLR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PBAK_EXEC) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picture Background"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PBAK_EXEC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PBAK_TXTR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picture Background Texturing"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PBAK_TXTR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PBAK_TXTR2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picture Background From File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PBAK_TXTR2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PBAK_bmp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Bitmap Filename"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PBAK_bmp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PBAK_jpg) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "JPEG Filename"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PBAK_jpg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PBAK_png) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "PNG Filename"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PBAK_png__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCALC_acos) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ArcCosine"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCALC_acos__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCALC_add) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCALC_add__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCALC_asin) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ArcSine"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCALC_asin__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCALC_cos) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cosine"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCALC_cos__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCALC_deri) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Differentiate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCALC_deri__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCALC_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Divide"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCALC_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCALC_exp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Exponentiate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCALC_exp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCALC_intg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Integrate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCALC_intg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCALC_log) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Natural Log"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCALC_log__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCALC_mult) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Multiply"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCALC_mult__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCALC_sin) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sine"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCALC_sin__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCIRC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Dimensions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCIRC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PCROSS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cross Product"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PCROSS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDANL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDANL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDANL_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Assign"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDANL_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDANL_WARN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDANL_WARN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDBBM_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "BBM options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDBBM_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCCD_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "CCD options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCCD_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCDF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "CumulativeDF"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCDF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCFLD_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Correl Field"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCFLD_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCLR_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Clear & Reset"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCLR_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCMAT_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Correl Matrix"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCMAT_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCMAT_MATX) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Correl Matrix"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCMAT_MATX__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCMAT_SNGL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Correl Matrix"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCMAT_SNGL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCORR_ADD) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Correlation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCORR_ADD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCORR_DEL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Correlations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCORR_DEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCORR_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Correlation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCORR_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCORR_PRE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Correlation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCORR_PRE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDCORR_WARN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDCORR_WARN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDIS_BETA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDIS_BETA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDIS_EXPO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDIS_EXPO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDIS_GAMA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDIS_GAMA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDIS_GAUS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDIS_GAUS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDIS_LOG1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDIS_LOG1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDIS_LOG2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDIS_LOG2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDIS_TGAU) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDIS_TGAU__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDIS_TRIA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDIS_TRIA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDIS_UNIF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDIS_UNIF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDIS_WEIB) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDIS_WEIB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDMCS_ALL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDMCS_ALL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDMCS_AUTO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Autostop Opt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDMCS_AUTO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDMCS_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Method Optns"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDMCS_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDOEL_BBM0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDOEL_BBM0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDOEL_BBM1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "PDDOEL options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDOEL_BBM1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDOEL_BBM2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "PDDOEL options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDOEL_BBM2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDOEL_CCD0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDOEL_CCD0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDOEL_CCD1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "PDDOEL options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDOEL_CCD1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDDOEL_CCD2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "PDDOEL options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDDOEL_CCD2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Map onto Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEF_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEF_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEF_clear) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clear Path Items"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEF_clear__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_ENTRYP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Run Parallel"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_ENTRYP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_ENTRYS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Run Serial"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_ENTRYS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_MCS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Run"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_MCS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_PAR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Run Parallel"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_PAR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_PAR_AF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Addit'l Files"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_PAR_AF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_PAR_HP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Host Select"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_PAR_HP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_RSM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Run"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_RSM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_SER) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Run Serial"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_SER__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_SERCPY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Run Serial"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_SERCPY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_SERRUN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Run Serial"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_SERRUN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_WARN1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_WARN1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_WARN2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_WARN2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDEXE_WARN3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDEXE_WARN3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDHIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Histogram"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDHIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDINQR_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Inquire"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDINQR_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDLHS_ALL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDLHS_ALL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDLHS_AUTO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Autostop Opt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDLHS_AUTO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDLHS_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Method Optns"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDLHS_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDMETH_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Method/Tool"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDMETH_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDMETH_MCS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Monte Carlo Sims"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDMETH_MCS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDMETH_RSM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Response Surface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDMETH_RSM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDMETH_WIZ) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Method Wizard"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDMETH_WIZ__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Dot Product"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDPINV) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Inverse Prob"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDPINV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDPLOT_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDPLOT_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDPROB) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Probabilities"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDPROB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDREP_OPT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Report Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDREP_OPT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDREP_WRT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Generate Report"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDREP_WRT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDRESU) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Resume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDRESU__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDSAVE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Save"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDSAVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDSCAT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Scatter Plot"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDSCAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDSENS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Sensitivities"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDSENS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDSHIS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Sampl History"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDSHIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDUSER_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "User File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDUSER_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDVAR_ADDRP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Random Output"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDVAR_ADDRP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDVAR_ADDRV) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDVAR_ADDRV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDVAR_EDITRV) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDVAR_EDITRV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDVAR_RP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Random Output"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDVAR_RP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PDVAR_RV) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Random Input"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PDVAR_RV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PD_AnFile) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Create"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PD_AnFile__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PD_NOPRM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PD_NOPRM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PD_STATUS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PD_STATUS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PERBC2D) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7)+((VECTORP+ELEM2D+edgeflx+edgeelc)*NOFILTER))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Periodic BCs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PERBC2D__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PERBC2DI) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PERBC2DI__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PERBC2D_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Periodic BCs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PERBC2D_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PERBC2D_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Periodic BCs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PERBC2D_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PERBC2D_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Periodic BCs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PERBC2D_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PFACT_mult) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (MULTIPNT*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Calculate PF"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PFACT_mult__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PFACT_psd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PSDRANDM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Calculate PF"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PFACT_psd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PFEDEF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "FE Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PFEDEF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PGSE_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Also select facets"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PGSE_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PGSE_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reselect facets"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PGSE_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PGSE_S) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select facets"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PGSE_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PGSE_U) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unselect facets"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PGSE_U__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PHFDEF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "HF Near Field"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PHFDEF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PHFDEF_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PHFDEF_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLCAMPBELL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Campbell"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLCAMPBELL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLCRACK) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+STRUCTRL+pmethod)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Crack/Crush"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLCRACK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLDISP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Deformed Shape"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLDISP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLESOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLESOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLESOL_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLESOL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLETAB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Elem Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLETAB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLETAB_dummy) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elem Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLETAB_dummy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLF2D) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+MAGNETIC+ELEM2D)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((edgeflx+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "2D Flux Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLF2D__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLHFFAR_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Far Field"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLHFFAR_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLHFFAR_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLHFFAR_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLLS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line Elem Res"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLLS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLNSOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLNSOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLNSOL_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLNSOL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLORBIT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot orbit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLORBIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLPAGM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Geometry"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLPAGM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLPATH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Graph"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLPATH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLPATH_dummy) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Path Items"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLPATH_dummy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLRECALL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Recall Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLRECALL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLSECT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Linearized Strs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLSECT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLSECT_dummy) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lineariz Strs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLSECT_dummy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLTRAC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+CFD)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Flow Tra"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLTRAC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLTRAC_00) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+ELECTRIC*MAGNETIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Particle Trace"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLTRAC_00__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLTRAC_EM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+ELECTRIC*MAGNETIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Particle Trace"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLTRAC_EM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLVAR_p26) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Graph Variables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLVAR_p26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLVECT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Predefined"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLVECT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLVECT2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Predefined"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLVECT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PLVECT_user) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "User-defined"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PLVECT_user__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMGTRAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26+MAGNETIC+harmonic)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (edgeelc*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Magnetics"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMGTRAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMGTRAN_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMGTRAN_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMGTRAN_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMGTRAN_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMGTRAN_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMGTRAN_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMLOPT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMLOPT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMLOPTH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMLOPTH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMLOPTM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMLOPTM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMLOPT_CL1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMLOPT_CL1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMLOPT_CL2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMLOPT_CL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMLOPT_CLE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clear"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMLOPT_CLE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMLOPT_ST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMLOPT_ST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMLPAR_CL0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMLPAR_CL0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMLPAR_CL1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On ESYS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMLPAR_CL1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PMLSIZ) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "PML"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PMLSIZ__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_POLY_PTXY) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Irregular Poly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_POLY_PTXY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_POWERH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+MAGNETIC+HARMONIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Power Loss"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_POWERH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_Ci0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PPATH_Ci0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_Ci1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Circular Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PPATH_Ci1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_Ci2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Circular Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PPATH_Ci2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_Ci3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PPATH_Ci3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_Lc) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Location"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PPATH_Lc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_Lc_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PPATH_Lc_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_NE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PPATH_NE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_Nd) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PPATH_Nd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_Nd1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PPATH_Nd1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_WP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Working Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PPATH_WP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_WP0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Working Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PPATH_WP0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_WP1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Working Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PPATH_WP1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PPATH_WPE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PPATH_WPE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRANGE_p1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Path Range"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRANGE_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRCAMPBELL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Print Campbell"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRCAMPBELL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRED) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Predictor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRED__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRED2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRED2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRERR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((POST1+RESULTS)+(lsdyna))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (STRUCTRL*THERMAL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Percent Error"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRERR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRESOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LSDYNA*LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element Solution"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRESOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRETAB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Elem Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRETAB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRETAB_dummy) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elem Table Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRETAB_dummy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRI2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Vertices"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PRI2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRI2_2d) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Vertices"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PRI2_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRITER) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Iteration Summry"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRITER__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRNLD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+cfd)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Loads"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRNLD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRNSOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Solution"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRNSOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRNSOL_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Solution"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRNSOL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "PRO"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PROD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Multiply"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PROD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRORBIT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Print Orbit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRORBIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRPATH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Path Items"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRPATH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRPATH_dummy) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Path Items"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRPATH_dummy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRRECALL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Recall Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRRECALL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRRSOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+cfd)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reaction Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRRSOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRSECT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Linearized"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRSECT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRSECT_dummy) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Linearized Strs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRSECT_dummy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRVAR_p26) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Variables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRVAR_p26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PRVECT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Vector Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PRVECT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDFRQ_eras) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDFRQ_eras__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDFRQ_eras2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDFRQ_eras2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDFRQ_erasP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PSDRANDM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Erase Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDFRQ_erasP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDGRAPH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Graph PSD Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDGRAPH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDRES) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PSDRANDM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Calc Controls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDRES__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDSPL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PSDRANDM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Spatial Correlat"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDSPL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDUNIT_psd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Settings"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDUNIT_psd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDVAL_psd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "PSD vs Freq"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDVAL_psd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDVAL_psd1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDVAL_psd1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDVAL_psd2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDVAL_psd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDVAL_psd3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDVAL_psd3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDVAL_psd4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDVAL_psd4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSDWAV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PSDRANDM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Traveling Wave"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSDWAV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_a2n) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_a2n__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_a2on) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_a2on__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_a2ov) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_a2ov__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_an) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements in Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_an__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_aon) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements in Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_aon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_aov) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements in Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_aov__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_e2n) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_e2n__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_e2on) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_e2on__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_e2ov) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_e2ov__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_e2p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_e2p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_e3p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_e3p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_en) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_en__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_eon) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_eon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_eov) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_eov__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_ep) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_ep__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_l2n) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_l2n__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_l2on) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_l2on__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_l2ov) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_l2ov__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_ln) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements in Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_ln__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_lon) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements in Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_lon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_lov) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements in Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_lov__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_sel2n) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Selected Element"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_sel2n__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_sel2on) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Selected Element"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_sel2on__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_seln) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Selected Element"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_seln__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_selon) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Selected Element"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_selon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_selov) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Selected Element"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_selov__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_v2n) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elem in volu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_v2n__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_v2on) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elem in volu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_v2on__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_v2ov) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elem in volu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSME_v2ov__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_vn) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements in Volu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_vn__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_von) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements in Volu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_von__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSME_vov) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements in Volu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PSME_vov__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSecLd) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "1 Pretensn Sectn"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSecLd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSecLdAll) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "All Pret Sectns"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSecLdAll__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSecLdDel) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Section"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSecLdDel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSecLdDelAll) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Section Lds"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSecLdDelAll__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSecLdDelEX) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSecLdDelEX__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSecLdM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ">1 Pret Section"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSecLdM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSecLdM_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ">1 Pret Section"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSecLdM_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PSecLd_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "1 Pretensn Sectn"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PSecLd_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PVECT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Unit Vector"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PVECT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Perturb) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STATIC*SUBSTRUC*(TRNSIENT+FULL)*HARMONIC)+cfd)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Perturb"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Perturb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Phys_clear) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Clear"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Phys_clear__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Phys_dele) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Phys_dele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Phys_list) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Phys_list__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Phys_rea1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Phys_rea1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Phys_rea2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Read"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Phys_rea2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Phys_read) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Read"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Phys_read__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Phys_stat) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Phys_stat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Phys_writ) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Write"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Phys_writ__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pick_Beam) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Edit Custom"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pick_Beam__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pick_Beam_db) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Edit Common"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pick_Beam_db__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pick_Mats) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Material"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Pick_Mats__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pick_Mats2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pick_Mats2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PiezoT0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PiezoT0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PiezoTr) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Piezo Transd"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_PiezoTr__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PipeAdd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PipeAdd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PipeAddEdit2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add / Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PipeAddEdit2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PipeAddEdit3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PipeAddEdit3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PipeEdit) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PipeEdit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PipeFactors) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Flexibility Factors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PipeFactors__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PipeFactors2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Flexibility Factors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PipeFactors2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/ANGLE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Angle of Rotation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/ANGLE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/ANGLE_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/ANGLE_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/DIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Magnification"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/DIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/DIST_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/DIST_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/DIST_B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/DIST_B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/DSCALE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Displacement Scaling"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/DSCALE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/DSCALE_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/DSCALE_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/FACET) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Solid Model Facets"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/FACET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/FOCUS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Focus Point"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/FOCUS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/FOCUS_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/FOCUS_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/FOCUS_B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Focus Point"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/FOCUS_B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/FOCUS_C) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/FOCUS_C__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/GFO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Floating Point Format"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/GFO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/GRESUME) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Restore Plot Ctrls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/GRESUME__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/GSAVE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Save Plot Ctrls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/GSAVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/LIGHT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Light Source"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/LIGHT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/LIGHT1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/LIGHT1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/LIGHT2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/LIGHT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/LIGHT3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/LIGHT3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/LIGHT4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/LIGHT4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/NORMAL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Shell Normals"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/NORMAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/NORMAL_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/NORMAL_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/SEG) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To Segment Memory"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/SEG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/SEG1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To Segment Memory"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/SEG1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/SEG2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/SEG2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/SEG2A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/SEG2A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/SHOW) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To GRPH File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/SHOW__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/SHOW2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/SHOW2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/SHOW3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/SHOW3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/TRLCY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Translucency"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/TRLCY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/TRLZCAP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Pl_/TRLZCAP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/UDOCCN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Contour Legend"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/UDOCCN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/UDOCTX) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Text Legend"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/UDOCTX__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/UDOCZZ) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "User Legend Replot"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/UDOCZZ__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/VCONE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Perspective View"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/VCONE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/VCONE_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/VCONE_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/VCONE_B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/VCONE_B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/VSCALE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Vector Arrow Scaling"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/VSCALE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/VSCALE_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/VSCALE_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_/XFRM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_/XFRM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANCMERR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation Error"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANCMERR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANCNTR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Deformed Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANCNTR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANCNTR0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANCNTR0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANCNTR2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANCNTR2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANCYC) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Wave Mode Shape"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANCYC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANCYC0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Wave Mode Shape"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANCYC0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANCYC2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANCYC2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANDATA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animate Over Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANDATA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANDATA0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Animate Over Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANDATA0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANDATA2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "HIDDEN"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANDATA2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANDSCL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Deformed Shape"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANDSCL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANDSCL0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Animate Deformed Shape"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANDSCL0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANDSCL2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANDSCL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANDYNA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Dynamic Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANDYNA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANDYNA0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Dynamic Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANDYNA0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANDYNA2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANDYNA2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANHARM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Time-harmonic"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANHARM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANIM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Replay with Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANIM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANIMERR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation Error"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANIMERR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANISOS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Isosurfaces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANISOS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANISOS0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANISOS0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANISOS2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANISOS2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANMODE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Mode Shape"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANMODE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANMODE0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Animate Mode Shape"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANMODE0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANMODE2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANMODE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANPLTR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Particle Flow"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANPLTR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANPLTR0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+CFD*ELECTRIC*MAGNETIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Flow Tra"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANPLTR0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANPLTR1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANPLTR1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANQCNT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Q-Slice Contours"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANQCNT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANQCNT0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANQCNT0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANQCNT2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Pl_ANQCNT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANQCNT3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANQCNT3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANQVEC) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Q-Slice Vectors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANQVEC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANQVEC0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Predefined"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANQVEC0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANQVEC2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Pl_ANQVEC2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANQVEC3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANQVEC3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANRESU) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Restore Animation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANRESU__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANSAVE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Save Animation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANSAVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANTIME) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animate Over Time"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANTIME__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANTIME0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Animate Over Time"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANTIME0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ANTIME2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Animation data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ANTIME2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_AutoFit) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Automatic Fit Mode"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_AutoFit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_AutoFit_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_AutoFit_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_BCMERR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Banded Contour Map Error"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_BCMERR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_BCM_OK) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Banded Contour Map"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_BCM_OK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_BandCntrClr) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Banded Contour Colors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_BandCntrClr__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_BandCntrMap) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Banded Contour Map"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_BandCntrMap__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ClrBC) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "BC Colors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ClrBC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ClrComp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Component Colors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ClrComp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ClrEntit1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ClrEntit1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ClrEntity) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Entity Colors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ClrEntity__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ClrGraph) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Graph Colors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ClrGraph__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ClrMap) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Load Color Map"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ClrMap__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ClrMapSave) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Save Color Map"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ClrMapSave__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ClrNumber) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Numbered Item Colors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ClrNumber__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ClrStress) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Contour Colors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ClrStress__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ClrWindow) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Window Colors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ClrWindow__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContLab) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Contour Labeling"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContLab__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContLab_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContLab_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContLab_C) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContLab_C__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContNon) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Non-uniform Contours"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContNon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContNon1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContNon1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContStyl1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContStyl1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContStyl2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContStyl2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContStyl3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContStyl3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContStyl4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContStyl4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContStyl5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContStyl5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContStyl6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContStyl6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContStyle) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Contour Style"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContStyle__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContUni) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Uniform Contours"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContUni__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContUni_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContUni_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ContUni_B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ContUni_B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Device) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Device Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Device__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Edge) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Edge Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Edge__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Edge_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Edge_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Graph0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Graph0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Graph1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Graph1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Graph10) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Graph10__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Graph2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Graph2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Graph3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Graph3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Graph4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Graph4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Graph9) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Graph9__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Hidden) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden-Line Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Hidden__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Hidden_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Hidden_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Hpgl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To HPGL File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Hpgl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Hpgl2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To HPGL2 File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Hpgl2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_IMMED) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Immediate Display"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_IMMED__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_JPEG) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_JPEG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ModAxes) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Axes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ModAxes__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ModCurv) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Curve"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ModCurv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_ModGrid) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Grid"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_ModGrid__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Number) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Numbering"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Number__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Number2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Number2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Orient) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Viewing Direction"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Orient__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Orient_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Orient_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_PNG) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_PNG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Pscr) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To PSCR File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Pscr__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Pscr_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Pscr_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_QUALITY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Best Quality Plot"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_QUALITY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_QUALITY1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_QUALITY1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Size) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Size and Shape"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Size__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Size_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Size_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Symbols) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Symbols"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Symbols__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Symbols2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Symbols2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Symbols3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Symbols3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Symbols4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Symbols4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Symbols5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Symbols5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Symbols5a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Symbols5a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Symbols5b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Symbols5b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Symbols5c) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Symbols5c__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Symbols6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Symbols6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_TIFF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_TIFF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Trans1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Trans1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Trans2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Trans2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_Trans3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_Trans3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_VRML) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_VRML__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_WinCopy) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Copy Window Specs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_WinCopy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_WinDele) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Window"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_WinDele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_WinLay) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Window Layout"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_WinLay__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_WinOnOff) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Window On or Off"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_WinOnOff__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_WinOpts) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Window Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_WinOpts__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_WinPick1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_WinPick1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pl_WinPick2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pl_WinPick2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Planar_Sym) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Planar Symmetry"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Planar_Sym__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_Dio_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_Dio_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_Dio_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden (Error Trap)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_Dio_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_Dio_4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_Dio_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_Dio_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_Dio_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_Dio_6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_Dio_6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_Diode) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Diode VI"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Plt_Diode__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_EMT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "EMT CDISP"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Plt_EMT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_EMT_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden (Error Trap)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_EMT_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_EMT_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_EMT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_EMT_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden (Error Trap)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_EMT_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_EMT_4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden (Error Trap)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_EMT_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_EMT_5) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_EMT_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_EMT_6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_EMT_6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_EMT_7) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_EMT_7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_Spr_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_Spr_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_Spr_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden (Error Trap)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_Spr_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_Spr_6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Plt_Spr_6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Plt_Spring) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Spring FD"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Plt_Spring__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PreSecModNam) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Name"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PreSecModNam__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PreSecModNor) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Normal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PreSecModNor__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_PreSecMod_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Normal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_PreSecMod_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pref_Msg1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Preferences"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pref_Msg1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pref_Msg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Preferences"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pref_Msg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Pref_Success) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Preferences"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Pref_Success__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Prefer_exec) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Preferences"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Prefer_exec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Prefer_exec2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Preferences"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Prefer_exec2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Preferences) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Preferences"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Preferences__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Preintegrate) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pre-integrated"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Preintegrate__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QDVAL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Quadspectral"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QDVAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QDVAL1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Quadspectral"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QDVAL1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QDVAL2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QDVAL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QDVAL3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QDVAL3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QDVAL4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QDVAL4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QESOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QESOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QESOL_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QESOL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QGSOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POWRGRPH)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Subgrid Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QGSOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QGSOL_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QGSOL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QNSOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (powrgrph)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QNSOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QNSOL_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QNSOL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QUAD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Quadratic Fill"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_QUAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QUAD2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QUAD2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QUADFREE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QUADFREE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QUADMAP34) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QUADMAP34__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QUADMAPA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QUADMAPA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QUASISTAT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((THERMAL+RADSURF)*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Quasi-Static"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QUASISTAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_QUOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Divide"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_QUOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_R) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add/Edit/Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RACE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+(PR_ELMAG*pr_set))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((edgeflx+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Racetrack Coil"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RACE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RADAR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "RCS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RADAR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RADAR_L) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "RCS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RADAR_L__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RADAR_N) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "RCS Normalized"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RADAR_N__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RADAR_NL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "RCS Normalized"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RADAR_NL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RADOPT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(RADSURF+THERMAL)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Radiosity Opts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RADOPT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RAPPND) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Write Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RAPPND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RBE3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+STRUCTRL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Dist F/M at Mstr"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RBE3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RBE3_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Dist F/M at Mstr"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RBE3_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RBREAD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "RBDOUT file"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RBREAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RCREAD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "RCFORC file"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RCREAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_REAL_p26) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST26+HARMONIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Real Part"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_REAL_p26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RECTNG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Dimensions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RECTNG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_REFLCOE1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_REFLCOE1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_REFLCOE2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_REFLCOE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_REFLCOEF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((POST1+RESULTS+EDGEELC+HARMONIC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Refl Coeff"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_REFLCOEF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_REFRESH) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "REFRESH"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_REFRESH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RESCONTROL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (STATIC*(TRNSIENT+FULL))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Restart Control"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RESCONTROL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RESCONTROL_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Restart Control"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RESCONTROL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RESET_p1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reset"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RESET_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RESET_p26) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reset Postproc"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RESET_p26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RESP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Generate Spectrm"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RESP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RFORCE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reaction Force"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RFORCE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RFORCE1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reaction Force"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RFORCE1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RFORCE1A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reaction Force"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RFORCE1A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RFORCE1e) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reaction Force"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RFORCE1e__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RFORCE_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RFORCE_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RFORCE_Ae) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RFORCE_Ae__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RFORCE_e) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reaction Force"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RFORCE_e__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Specified Real Constants"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RMANL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Model Features"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RMANL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RMANL2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RMANL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RMANL3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RMANL3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RMFLVEC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+MODAL+TFILM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Extract Eigv"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RMFLVEC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RMLVSCALE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (ROMES+PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "For ROM"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RMLVSCALE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RMLVSCALE_dele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (ROMES+PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "For ROM"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RMLVSCALE_dele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RMNDISP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Extract NP DISP"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RMNDISP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RMNEVEC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+MODAL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Extract NP Eigv"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RMNEVEC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RMRESU) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Resume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RMRESU__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RMSAVE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Save"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RMSAVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RMXPORT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "VHDL-AMS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RMXPORT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ROMBRS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Database"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ROMBRS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ROMELM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ElecStruc"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ROMELM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ROMELM_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ElecStruc"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ROMELM_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ROM_VSweep) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna+ROMES)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Voltage Sweep"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ROM_VSweep__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPOLY) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Side Length"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RPOLY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPOLY_major) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Circumscr Rad"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RPOLY_major__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPOLY_minor) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Inscribed Rad"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RPOLY_minor__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Triangular"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4_2d) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Triangle"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4_4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Square"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4_4_2d) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Square"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4_4_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4_5) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pentagonal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4_5_2d) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pentagon"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4_5_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4_6) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hexagonal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4_6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4_6_2d) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hexagon"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4_6_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4_7) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Septagonal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4_7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4_7_2d) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Septagon"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4_7_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4_8) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Octagonal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4_8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPR4_8_2d) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Octagon"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RPR4_8_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPRISM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Side Length"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RPRISM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPRISM_major) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Circumscr Rad"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RPRISM_major__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPRISM_minor) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Inscribed Rad"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RPRISM_minor__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPRT_GEN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Report Generator"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RPRT_GEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPSD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((POST26+PSDRANDM)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Calc Resp PSD"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RPSD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RPSD_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((POST26+PSDRANDM)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Calc Resp PSD"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RPSD_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSFIT1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Fit Resp Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSFIT1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSFIT2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Fit Resp Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSFIT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSFIT_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Fit Resp Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSFIT_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSOUT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Write Sim Dat"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSOUT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plt Resp Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSPLOT_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plt Resp Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSPLOT_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSPRNT1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Prn Resp Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSPRNT1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSPRNT2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Prn Resp Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSPRNT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSPRNT3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Prn Resp Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSPRNT3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSPRNT_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Prn Resp Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSPRNT_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSSIMS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "RS Simulation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSSIMS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSSIMS_CONT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSSIMS_CONT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSSIMS_ENTRY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "RS Simulation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSSIMS_ENTRY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSSIMS_INIT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSSIMS_INIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSSIMS_SEED) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Seed value"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSSIMS_SEED__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RSSIMS_TIME) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RSSIMS_TIME__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RS_NORSSET) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RS_NORSSET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RS_NOSOLSET) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RS_NOSOLSET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RTHICK) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Thickness Func"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RTHICK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_CreateCM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Create Rezone Component"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_CreateCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_DESIZE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Default Element Sizing Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_DESIZE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_DONE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (REMESHRZ)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Finish"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_DONE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_Finish) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (REMESHRZ)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cancel"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_Finish__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_KSCON) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Skew Mesh at Keypoint"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_KSCON__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_LCCAT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Concatenate Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_LCCAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_LCCAT_del) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (REMESHRZ)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Concatenated Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_LCCAT_del__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_LCOMB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (REMESHRZ)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Combine Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_LCOMB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_MshrOpts) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Global Meshing Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_MshrOpts__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_ReadFile) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (REMESHRZ)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Read Mesh"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_ReadFile__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_SelCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (REZNCM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Select Rezone Component"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_SelCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_SelElem) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Select Rezone Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_SelElem__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_SelNode) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Select Nodes to Maintain"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_SelNode__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_SelNode2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Maintain Node Information"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RZN_SelNode2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_Solve) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (MAPSRDRZ)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Map Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_Solve__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_SplitEl2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (REMESHRZ)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RZN_SplitEl2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_SplitEl3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (REMESHRZ)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Split element for rezoning"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RZN_SplitEl3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_SplitElm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (REMESHRZ)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Select and Split Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_SplitElm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RZN_Start) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION&remeshrz&mapsrdrz)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Start"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RZN_Start__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_R_solu) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Change RealConst"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_R_solu__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_R_too) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Real Constants"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_R_too__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RadOpt12) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (AUX12*PR_CFD*PR_THERM*(RADSURF+THERMAL)*CFD)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solution Opt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RadOpt12__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RadOpt12_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (AUX12*PR_CFD*PR_THERM*(RADSURF+THERMAL)*CFD)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RadOpt12_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RadOpt13) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (AUX12*PR_CFD*PR_THERM*(RADSURF+THERMAL)*CFD)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "View Factor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RadOpt13__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RadOpt13_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RadOpt13_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RadOpt13_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RadOpt13_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RaiseSectMen) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RaiseSectMen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ReadOpts_p1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Data & File Opts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ReadOpts_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RebarNorm) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Normal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RebarNorm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RebarOnly) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reinf Only"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RebarOnly__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RebarPlus) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reinf + Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RebarPlus__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ReduceGui) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((STATIC*TRNSIENT*BUCKLING*MODAL)+SIMPLOFF)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Abridged Menu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ReduceGui__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ReduceMatGui) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (cfd+pr_cfd+UNABRMAT)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Abridged Menu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ReduceMatGui__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ReinfAddEdit) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add / Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ReinfAddEdit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RemoveDialogs) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RemoveDialogs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Restart) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STATIC*SUBSTRUC*(TRNSIENT+FULL)*HARMONIC)+cfd)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Restart"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Restart__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ResultView) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Results Viewer"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ResultView__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RetBox) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RetBox__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RetBox_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RetBox_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RetBox_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RetBox_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomAAC2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomAAC2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomAAC3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomAAC3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomAAC4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RomAAC4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomAAC5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RomAAC5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomAddAllCap) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "All Capacitances"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomAddAllCap__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomAddSingCap) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Single Capacitance"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomAddSingCap__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomCapList) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomCapList__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomDefMN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Define"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RomDefMN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomDefRS2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RomDefRS2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomDefineRS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomDefineRS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomDelCap) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomDelCap__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomDelMN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_RomDelMN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomDelMN_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomDelMN_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomElementLoad) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element Load"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomElementLoad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomFitRS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Fit Functions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomFitRS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomListMN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomListMN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomModalAnalysis) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Run Modal Anal."]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomModalAnalysis__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomModeList) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomModeList__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomModeList2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomModeList2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomModeS2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomModeS2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomModeSAuto) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomModeSAuto__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomModeSManual) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomModeSManual__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomPlotRS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomPlotRS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomPolyOrder) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Poly Order"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomPolyOrder__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomRSStatus) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomRSStatus__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomRunSamples) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Compute Points"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomRunSamples__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RomTestLoad) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Test Load"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RomTestLoad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RotCoriolis) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Coriolis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RotCoriolis__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Rot_Dam_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Rot_Dam_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Rot_Dam_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Rot_Dam_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Rot_Dam_No) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlin Rotary"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Rot_Dam_No__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Rot_Damper) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Linear Rotary"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Rot_Damper__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Rot_Spr_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Rot_Spr_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Rot_Spr_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Rot_Spr_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Rot_Spr_No) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG+profs)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlin Rotary"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Rot_Spr_No__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Rot_Spring) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Linear Rotary"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Rot_Spring__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_RunStatFil) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list runstat]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Run Stat"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_RunStatFil__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SABS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Abs Value Option"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SABS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SADD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add Items"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SADD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SALLOW) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Constant"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SALLOW__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SALLOW_reset) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reset Stress"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SALLOW_reset__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SBCTRAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Solid Lds"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SBCTRAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SCENERGY) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+MAGNETIC+edgeelc)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Co-Energy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SCENERGY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SCENERGY_H) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SCENERGY_H__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SCENERGY_S) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SCENERGY_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SCLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Specified Section Numbers"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SCLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SDELETE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Section"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SDELETE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From .SUB File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SECCONreinf) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Sect Control"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SECCONreinf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SECPLOTbeam) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Section"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SECPLOTbeam__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SECPLOTfit) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Section Plot"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SECPLOTfit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SECPLOTpipe) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Section"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SECPLOTpipe__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SECPLOTrein2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Section"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SECPLOTrein2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SECPLOTreinf) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Section"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SECPLOTreinf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SECPLOTshel) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Section"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SECPLOTshel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEDLIS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((POST1)+(lsdyna))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Superelem DOF"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SEDLIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEEXP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SUBSTRUC))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Expand Superelem"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SEEXP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Automatic"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SEGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEGEN_2a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Superelement Generation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SEGEN_2a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEGEN_2b) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Superelement Generation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SEGEN_2b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEGEN_2c) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Superelement Generation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SEGEN_2c__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SELIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Superelem Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SELIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SENERGY) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+MAGNETIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Energy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SENERGY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SENERGY_H) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SENERGY_H__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SENERGY_S) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SENERGY_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEQ_Sequen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sequential method"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SEQ_Sequen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEQ_Simul) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Simul"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SEQ_Simul__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SESYMM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Reflection"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SESYMM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SETFGAP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION+TFILM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ThinFilm"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SETFGAP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SETFGAP_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION+TFILM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ThinFilm"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SETFGAP_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SETMOD) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SETMOD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SETRAN_cs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By CS Transfer"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SETRAN_cs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SETRAN_geom) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Geom Offset"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SETRAN_geom__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_detailed) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Detailed Summary"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_detailed__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_detl_ord) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((MODAL*BUCKLING)+CYCLIC)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Detailed Summary (Freq ordered)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_detl_ord__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_first) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "First Set"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_first__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_last) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Last Set"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_last__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_list) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Pick"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_list__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_list_ord) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((MODAL*BUCKLING)+CYCLIC)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Pick (Freq ordered)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_list_ord__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_lstep) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Load Step"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_lstep__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_next) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Next Set"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_next__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_nset) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Set Number"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_nset__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_prev) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Previous Set"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_prev__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_sum_ord) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((MODAL*BUCKLING)+CYCLIC)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Results Summary (Freq ordered)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_sum_ord__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_summary) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Results Summary"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_summary__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SET_time) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Time/Freq"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SET_time__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEW_Ar) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SEW_Ar__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEW_Li) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SEW_Li__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEW_Vo) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SEW_Vo__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SEXP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Exponentiate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SEXP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SE_Man_EXP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Expansion Pass"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SE_Man_EXP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SE_Man_GEN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Generation Pass"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SE_Man_GEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SE_Man_USE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Use Pass"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SE_Man_USE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFACT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "SF for Node Strs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFACT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFACT_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Restore NodeStrs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFACT_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_FSInt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_FSInt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFADELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_chr2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFADELE_chr2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_con2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFADELE_con2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_conv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_conv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_frsf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_frsf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_fsi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_fsi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_fsi2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFADELE_fsi2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_hfl2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFADELE_hfl2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_hflu) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_hflu__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_imp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFADELE_imp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_impE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_impE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_impd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_impd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_inf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+edgeflx*ELECSTAT)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_inf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_inf2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFADELE_inf2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_mci) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((CIRC+VECTORP+ELEM3D+MAGNETIC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_mci__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_mxwf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC*EDGEFLX*ELECSTAT)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_mxwf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_port) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_port__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_pre2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFADELE_pre2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_rad) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS+THERMAL+FLOTRAN+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_rad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_rad2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFADELE_rad2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_rdmx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_rdmx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADELE_shld) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADELE_shld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADEL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFADEL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFADEL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFADEL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFALIS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFALIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_FSInt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_FSInt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_chrgs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_chrgs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_chrgs2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFA_chrgs2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_conv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_conv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_frsf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_frsf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_frsf2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFA_frsf2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_fsi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_fsi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_hflux) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_hflux__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_impd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_impd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_impd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFA_impd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_impd4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFA_impd4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_impdE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_impdE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_inf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+edgeflx*ELECSTAT)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_inf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_mci) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((CIRC+VECTORP+ELEM3D+MAGNETIC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_mci__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_mci2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFA_mci2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_mxwf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC*EDGEFLX*ELECSTAT+edgeelc+harmonic*ELEM2D)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_mxwf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFA_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_port) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_port__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_port1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFA_port1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_port2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFA_port2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_rad) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL+LINPLUS+FLOTRAN+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_rad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_rad2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFA_rad2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_radF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL+LINPLUS+FLOTRAN+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_radF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_rdmx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_rdmx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_rdmx2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFA_rdmx2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_shld) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFA_shld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFA_shld4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFA_shld4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFBEAM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Beams"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFBEAM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFBEAM2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFBEAM2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFCALC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "SF for ElemTable"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFCALC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFCUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Surface Loads"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFCUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_FSInt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_FSInt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_conv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_conv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_frsf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_frsf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_fsi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_fsi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_hflu) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_hflu__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_impd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_impd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_impdE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_impdE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_inf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+edgeflx*ELECSTAT)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_inf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_mci) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((CIRC+VECTORP+ELEM3D+MAGNETIC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_mci__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_mxwf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC*EDGEFLX*ELECSTAT)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_mxwf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_port) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_port__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_prCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Node Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_prCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_rad) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL+FLOTRAN+LINPLUS+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_rad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_rdmx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_rdmx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDELE_shld) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDELE_shld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDEL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDEL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDEL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFDEL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFDEL_vf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFDEL_vf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE3_conv) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE3_conv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDELE_FSInt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDELE_FSInt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDELE_selv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "For Superelement"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDELE_selv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_chrg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDEL_chrg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_conv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_conv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_conv2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDEL_conv2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_frsf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_frsf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_frsf2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDEL_frsf2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_hflu) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_hflu__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_hflu2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDEL_hflu2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_impd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_impd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_impd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDEL_impd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_impdE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_impdE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_inf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+edgeflx*ELECSTAT)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_inf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_mci) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((CIRC+VECTORP+ELEM3D+MAGNETIC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_mci__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_mxwf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC*EDGEFLX*ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_mxwf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDEL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_pr2CM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDEL_pr2CM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_prCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Element Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_prCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_pres2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDEL_pres2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_rad) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL+FLOTRAN+LINPLUS+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_rad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_rad2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDEL_rad2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_rdmx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_rdmx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_rdmx2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDEL_rdmx2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_vf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFEDEL_vf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFEDEL_vf2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFEDEL_vf2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFELIS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFELIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_FSInt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_FSInt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_VFRC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+CFD*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_VFRC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_chrgs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_chrgs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_chrgs2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE_chrgs2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_convt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+cfd)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Tapered"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_convt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_convt2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE_convt2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_convt3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE_convt3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_convu) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Uniform"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_convu__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_frsf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_frsf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_frsf2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE_frsf2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_hflux) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_hflux__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_impd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_impd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_impd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE_impd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_presCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Element Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_presCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_rad) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL+LINPLUS+FLOTRAN+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_rad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_rad2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE_rad2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_rad3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE_rad3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_radF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL+LINPLUS+FLOTRAN+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_radF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_rdmx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFE_rdmx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_rdmx1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE_rdmx1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_rdmx2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE_rdmx2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFE_selv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "For Superelement"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFE_selv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFFUN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Node Function"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFFUN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFGRAD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Gradient"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFGRAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_FSInt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_FSInt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFLDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_conv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_conv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_frsf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_frsf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_fsi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_fsi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_hflu) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_hflu__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_impd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_impd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_inf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+edgeflx*ELECSTAT)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_inf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_mxwf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC*EDGEFLX*ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_mxwf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_rad) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL+LINPLUS+FLOTRAN+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_rad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDELE_rdmx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDELE_rdmx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDEL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDEL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDEL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFLDEL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLDEL_vf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLDEL_vf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFLLIS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFLLIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_FSInt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_FSInt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_VFRC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_VFRC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_chrgs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_chrgs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_chrgs2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFL_chrgs2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_conv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_conv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_frsf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_frsf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_frsf2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFL_frsf2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_fsi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_fsi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_hflux) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_hflux__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_impd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_impd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_impd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFL_impd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_inf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+edgeflx*ELECSTAT)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_inf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_mxwf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC*EDGEFLX*ELECSTAT+edgeelc+harmonic*ELEM2D)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_mxwf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_rad) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL+LINPLUS+FLOTRAN+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_rad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_rad2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFL_rad2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_radF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL+LINPLUS+FLOTRAN+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_radF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_rdmx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SFL_rdmx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFL_rdmx2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFL_rdmx2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFSCALE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Surface Loads"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFSCALE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SFTRAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Surface Loads"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SFTRAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_FSInt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_FSInt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_chrgs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_chrgs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_chrgs2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_chrgs2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_conv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_conv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_frsf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_frsf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_frsf2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_frsf2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_fsi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_fsi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_hflux) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_hflux__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_impd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_impd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_impd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_impd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_impd4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_impd4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_impdE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_impdE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_inf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+edgeflx*ELECSTAT)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_inf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_mci) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((CIRC+VECTORP+ELEM3D+MAGNETIC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_mci__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_mci2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_mci2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_mxwf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC*EDGEFLX*ELECSTAT)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_mxwf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_port) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_port__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_port1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_port1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_port2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_port2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_pres1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_pres1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_presCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Node Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_presCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_rad) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL+LINPLUS+FLOTRAN+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_rad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_rad2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_rad2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_radF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL+LINPLUS+FLOTRAN+FULL_ANS)*(FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_radF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_rdmx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_rdmx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_rdmx2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_rdmx2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_shld) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SF_shld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SF_shld4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SF_shld4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SHPP_change) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SHPP_change__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SHPP_toggle) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Toggle Checks"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SHPP_toggle__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SHPP_toggle2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Toggle Checks"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SHPP_toggle2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List Sections"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SLOAD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list STRUCTRL]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pretnsn Sectn"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SLOAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SLREAD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "SLEOUT file"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SLREAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SMALL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Find Minimum"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SMALL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SMAX) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Find Maximum"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SMAX__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SMIN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Find Minimum"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SMIN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SMOOTH_data) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION*PREP7*POST26)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Smooth Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SMOOTH_data__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SMULT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Multiply"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SMULT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SOLCONTROL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (pr_cfd+cfd+magnetic*STRUCTRL*THERMAL*MULTIFLD)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solution Ctrl"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SOLCONTROL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SOLCONTROL_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlinear Solution Control"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SOLCONTROL_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SOLCONTROL_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlinear Solution Control"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SOLCONTROL_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SOLC_VolChk) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mixed U-P Toler"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SOLC_VolChk__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SOLU) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solution Summary"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SOLU__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SOLVE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (pp*LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Current LS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SOLVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SOLVE2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SOLVE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SOLVEDYN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (pp)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solve"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SOLVEDYN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SOLVE_PERT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Perturb LS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SOLVE_PERT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SOLVE_PERT2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Perturb LS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SOLVE_PERT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPCNOD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((RADSURF+THERMAL)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Space Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPCNOD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPCNOD_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPCNOD_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPCREAD) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "SPCFORC file"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SPCREAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPCTEMP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((RADSURF+THERMAL)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Space Temp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPCTEMP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPCTEMP_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPCTEMP_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPFREQ) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Freq points"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPFREQ__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPFREQ_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPFREQ_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPFREQ_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPFREQ_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPFRQ_eras) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (MULTIPNT*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Erase Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPFRQ_eras__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPFRQ_eras2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPFRQ_eras2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPGRAPH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(MULTIPNT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Graph MPRS Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPGRAPH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPH4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solid Sphere"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SPH4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPH4_hollow) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hollow Sphere"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SPH4_hollow__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPH4_iges) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solid Sphere"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SPH4_iges__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPH5) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By End Points"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SPH5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPHERE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Dimensions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPHERE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPLINE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Segmented Spline"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SPLINE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPLINE_end) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Segmented Spline"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SPLINE_end__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPLINE_op) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Segmented Spline"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SPLINE_op__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPOINT_node) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "At Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SPOINT_node__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPOINT_xyz) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "At XYZ Loc"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SPOINT_xyz__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPUNIT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(MULTIPNT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Settings"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPUNIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPVAL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(MULTIPNT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Spect vs Freq"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPVAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SPVAL_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SPVAL_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SQRT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Square Root"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SQRT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SRCS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+VECTORP+STATIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((edgeflx+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Terminal Par"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SRCS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SSLN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sel Small Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SSLN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SSUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sum of Each Item"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SSUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_/aux12) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Radiation Matrix"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_/aux12__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_/opt) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Design Opt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_/opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_Dynstat) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "LS-DYNA"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_Dynstat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_atyp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Analysis Type"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_atyp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_bf) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Body Loads"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_bf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_bioo) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Biot Savart Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_bioo__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_calc_p1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Calculations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_calc_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_ceqn) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Constraint Eqns"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_ceqn__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_cnfg) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Configuration"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_cnfg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_coup) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Coupled Sets"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_coup__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_d) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "DOF Constraints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_data_p1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_data_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_deac) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elem Birth/Death"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_deac__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_def_p26) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Variables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_def_p26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_defi_p1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Read Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_defi_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_disp_p1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_disp_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_dyno) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Dynamics Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_dyno__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_elem) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_elem__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_etyp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element Types"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_etyp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_f) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Forces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_f__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_fals) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_fals__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_fati_p1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Fatigue Calcs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_fati_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_gap) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Gap Conditions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_gap__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_geno) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "General Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_geno__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_inrt) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Inertia Loads"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_inrt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_lcca_p1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Load Case Calcs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_lcca_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_lsop) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Load Step Operations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_lsop__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_mast) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Master DOF"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_mast__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_mate) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Materials"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_mate__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_nlop) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlinear Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_nlop__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_no1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_no1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_no2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_no2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_no3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_no3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_node) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_node__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_outo) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Output Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_outo__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_path_p1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Path Operations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_path_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_plo_p26) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_plo_p26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_poin_p1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Trace Points"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_poin_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_pri_p26) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_pri_p26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_prin_p1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_prin_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_rcon) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Real Constants"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_rcon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_reor) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reorder Module"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_reor__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_selm) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Superelements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_selm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_sf) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Surface Loads"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_sf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_solid) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Solid Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_solid__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_solu) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "General"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_solu__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_sort_p1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Sort Module"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_sort_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_spec_p1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Output Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_spec_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_spto) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Spectrum Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_spto__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_tble) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Data Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_tble__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_true) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_true__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_yes1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_yes1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_yes2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_yes2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_yes3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_yes3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STAT_yes4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STAT_yes4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STEFBOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((RADSURF+THERMAL)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "General Opts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STEFBOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STORE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Store Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STORE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_STRSOPT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Stress Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_STRSOPT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUBSET) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Read Selected"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUBSET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUBSET_l) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUBSET_l__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUBSET_ll) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUBSET_ll__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUBSET_ln) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUBSET_ln__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUBSET_n) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUBSET_n__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUBSET_nl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUBSET_nl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUBSET_nn) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUBSET_nn__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_abs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Absolute"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_abs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_acos) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ArcCosine"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_acos__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_add) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_add__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_asin) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ArcSine"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_asin__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_atan) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ArcTangent"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_atan__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_atan2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ArcTangent2"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_atan2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_cos) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cosine"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_cos__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Divide"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_exp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Exponentiate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_exp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_init) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Initialize"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_init__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_log) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Natural Log"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_log__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_mult) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Multiply"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_mult__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_sin) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sine"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_sin__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCALC_sub) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Subtract"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCALC_sub__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCR_SPH1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "At Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCR_SPH1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCR_SPH2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Create Sphere Result Surface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SUCR_SPH2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCR_SPH3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Create Sphere Result Surface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCR_SPH3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCR_by_dim) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Dimensions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCR_by_dim__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCR_comp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCR_comp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCR_comp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCR_comp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCR_cplane) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Cuttng Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCR_cplane__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCR_cplane2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Cuttng Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SUCR_cplane2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCR_elem) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SUCR_elem__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCR_elem2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Create Surface on elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCR_elem2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUCR_infc) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Inf. Cylinder"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUCR_infc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUDEL_surf) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Surfaces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUDEL_surf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUEVAL_avg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Average Result"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUEVAL_avg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUEVAL_intg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Integrate Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUEVAL_intg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUEVAL_sum) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sum of Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUEVAL_sum__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUGET) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Results to Array"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUGET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUMAP_itm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Map Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUMAP_itm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUMAP_srfclr) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clear Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUMAP_srfclr__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUPL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUPL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUPL_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUPL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUPL_rset) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUPL_rset__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUPL_srf) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUPL_srf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUPL_vect) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Vectors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUPL_vect__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUPR_glb) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUPR_glb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUPR_srfs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Print Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUPR_srfs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SURESU) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Resume Surfaces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SURESU__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUSAVE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Save Surfaces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUSAVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUSEL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select Surfaces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUSEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUVECT_cross) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cross Product"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUVECT_cross__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUVECT_dot) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Dot Product"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUVECT_dot__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SUVECT_smult) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Scale Result"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SUVECT_smult__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SINGLEPT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Spectr Values"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SV0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SV0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SV1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SV1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SV2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SV2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SVTYP_etc) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SINGLEPT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Settings"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SVTYP_etc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SV_stat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SINGLEPT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Show Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SV_stat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SWADD3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add More Surfaces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SWADD3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SWADD_an) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SWADD_an__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SWADD_cn) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Node Component"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SWADD_cn__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SWDEL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SWDEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SWDLIS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "SpotWeld Solution"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SWDLIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SWGEN2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create New SpotWeld"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SWGEN2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SWGEN3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create New SpotWeld"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SWGEN3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SWGEN_an) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SWGEN_an__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SWGEN_cn) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Node Component"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_SWGEN_cn__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SWLIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SWLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SXFil) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list vt]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "VT"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SXFil__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SYNCRO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Synchronous"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SYNCRO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SecLib) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Library Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SecLib__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SecRead) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Import Library"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SecRead__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SecWrite_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Write From Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SecWrite_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SecWrite_B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Write Section"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SecWrite_B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SecWrite_F) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "SECWRITE"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SecWrite_F__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SecWrite_X) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SecWrite_X__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SectControl) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sect Control"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SectControl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Sect_EREF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Refine Mesh"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Sect_EREF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Sect_EREF_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Refine Mesh at ELEM"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Sect_EREF_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Sect_EREF_ex) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Sect_EREF_ex__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_SetUp_aux12) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (AUX12)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (pr_cfd+cfd)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Other Settings"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_SetUp_aux12__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ShellAddEdit) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add / Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ShellAddEdit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Shpp_Check) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)+(igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Shape Checking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Shpp_Check__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Slide_Film) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Slide Film"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Slide_Film__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Slide_Film_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Slide_Film_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Spec_Exit) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Exit Species"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Spec_Exit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Spec_VZ) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Spec_VZ__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_StLoad) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_StLoad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_StLoad2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_StLoad2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_StLoad4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_StLoad4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_StLoad50) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_StLoad50__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_StLoad6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_StLoad6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_StLoad7) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_StLoad7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_StLoad8) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_StLoad8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_StLoad9) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_StLoad9__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Staddtbl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Staddtbl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Stat_Mesh) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Surface Mesh Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Stat_Mesh__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Status) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Symmetry Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Status__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Stld) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Stld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Stnewtbl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Stnewtbl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Stnewtbl2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Stnewtbl2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Stnewtbl3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Stnewtbl3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Stoldtable) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Stoldtable__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Strain_Rate) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Strn Rate Effect"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Strain_Rate__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Stvalue) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Stvalue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TALLOW) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Temp-depend"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TALLOW__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TALLOW2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TALLOW2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TALLOW_reset) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reset Temps"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TALLOW_reset__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define/Activate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBCOPY) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Copy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBCOPY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBDELE_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBDELE_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBLIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBLIST_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBLIST_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBLIST_A) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBLIST_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBLIST_B) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBLIST_B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Graph"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBPLOT_ex1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Data Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBPLOT_ex1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBPLOT_ex2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Data Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBPLOT_ex2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBPLOT_ex3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Data Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBPLOT_ex3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBTEMP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Set Temp Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBTEMP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TBTEMP_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Set Temp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TBTEMP_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TB_edit) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Edit Active"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TB_edit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TB_mooney) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TB_mooney__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TB_status) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TB_status__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TCHG) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Change Tets"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TCHG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TCHG2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TCHG2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TCHG3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Change Tets"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TCHG3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TCHG4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TCHG4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TDMA_Warn) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "TDMASeep >1 Warn"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TDMA_Warn__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TETFREE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TETFREE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TIME_sub) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STATIC*TRNSIENT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Time and Substeps"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TIME_sub__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TIME_sub2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TIME_sub2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TIME_time) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STATIC*TRNSIENT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Time - Time Step"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TIME_time__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TIME_time2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TIME_time2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TIMINT_TINT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(TRNSIENT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Amplitude Decay"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TIMINT_TINT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TIMINT_TINT2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(TRNSIENT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Newmark Parameters"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TIMINT_TINT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TIMP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Detached Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_TIMP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TIMP_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Detached Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TIMP_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TLSPARM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((POST1+RESULTS+EDGEELC+HARMONIC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Trans Line S-Par"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TLSPARM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TLSWP1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TLSWP1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TLSWP2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TLSWP2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TORQ2D) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+ELEM2D+VECTORP+harmonic)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((edgeflx+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Torque"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TORQ2D__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TORQC2D) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+ELEM2D+VECTORP)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((edgeflx+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Circular Torq"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TORQC2D__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TORQSUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+FMAGBC+ELEM2D)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Torque"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TORQSUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TORQSUM_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TORQSUM_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TORQSUM_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TORQSUM_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TORQSUM_4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TORQSUM_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TORUS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Torus"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TORUS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRANS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_TRANS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRANS_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TRANS_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TREF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*FLUID*THERMAL*STRUCTRL*LSDYNA)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reference Temp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TREF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRIFREE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TRIFREE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRIMAP34) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TRIMAP34__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRIMAPA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TRIMAPA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRPDEL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+CFD*ELECTRIC*MAGNETIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Dele Trace Pt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_TRPDEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRPLIS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+CFD*ELECTRIC*MAGNETIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Trace Pt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_TRPLIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRPLIS_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List Trace Pt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TRPLIS_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRPOIN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+CFD*ELECTRIC*MAGNETIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Defi Trace Pt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_TRPOIN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRPOIN_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Defi Trace Pt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TRPOIN_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRPOIN_cs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "..in Activ CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TRPOIN_cs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TRTIME) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+CFD*ELECTRIC*MAGNETIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Time Interval"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TRTIME__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TSRES_30) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TSRES_30__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TSRES_31) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TSRES_31__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TSRES_311) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TSRES_311__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TSRES_32) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TSRES_32__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TSRES_321) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TSRES_321__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TSRES_322) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TSRES_322__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TSRES_323) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TSRES_323__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TUNIF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LSDYNA*LINPLUS*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Uniform Temp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TUNIF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Taper_explct) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By XYZ Location"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Taper_explct__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Taper_pick) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picked Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Taper_pick__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Taper_pick_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picked Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Taper_pick_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Taper_pick_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picked Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Taper_pick_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Taper_pick_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picked Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Taper_pick_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Taper_pick_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picked Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Taper_pick_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TempUnits) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list ((LINPLUS*THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Temperature Units"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TempUnits__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_To_Be_Done) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "To Be Done"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_To_Be_Done__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_TopoFil) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list topoopt]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Topo Opt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_TopoFil__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Transduc_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Transduc_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Transducer) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ElecMech"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Transducer__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Trn_Dam_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Trn_Dam_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Trn_Dam_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Trn_Dam_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Trn_Dam_No) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlin Trans"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Trn_Dam_No__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Trn_Damper) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Linear Trans"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Trn_Damper__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Trn_Spr_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Trn_Spr_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Trn_Spr_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Trn_Spr_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Trn_Spr_No) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG+profs)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlin Trans"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Trn_Spr_No__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Trn_Spring) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Linear Trans"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Trn_Spring__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Tutorial) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Menu Name"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Tutorial__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Tutorial_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Menu Name"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Tutorial_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_UIMP_iso) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Isotropic"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_UIMP_iso__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_UIMP_iso_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Isotropic"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_UIMP_iso_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_UIMP_ortho) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Orthotropic"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_UIMP_ortho__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_UIMP_ortho2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Orthotropic"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_UIMP_ortho2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_UNADAPTED_ET) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 17x/4x"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_UNADAPTED_ET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_UNADPT_BETA0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 17x"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_UNADPT_BETA0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_UNDO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Session Editor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_UNDO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_UPMOD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)*(NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Updt Node Coord"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_UPMOD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_USRCAL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "User Routines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_USRCAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_User_Beam) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Read Sect Mesh"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_User_Beam__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_V) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Through KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_V__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VA53) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volume by Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VA53__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VADD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VADD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VADD_igs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VADD_igs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VAGeoRepair) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create Volume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VAGeoRepair__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VATT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VATT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VATT_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VATT_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VATT_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VATT_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VCLEAR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VCLEAR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VCNTRL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volu Cntrls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VCNTRL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VCROSS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cross Product"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VCROSS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes Only"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VDELE_sweep) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volume and Below"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VDELE_sweep__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VDGL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Degen Volus"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VDGL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VDOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Dot Product"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VDOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VDRAG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Along Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VDRAG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEORa) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Z Normal to Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VEORa__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEORa2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VEORa2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEORdel) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Specification"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VEORdel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEORdel2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VEORdel2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEORk) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Z by 2 Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VEORk__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEORk2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VEORk2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEORl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Z Along Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VEORl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEORl2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VEORl2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEORth) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Z in Thin Direction"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VEORth__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEORth2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Thin"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VEORth2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEXT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By XYZ Offset"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VEXT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VEXT_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By XYZ Offset"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VEXT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFCALC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (AUX12*PR_CFD*PR_THERM*(RADSURF+THERMAL)*CFD)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Compute"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VFCALC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFOPTS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((RADSURF+THERMAL)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VFOPTS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFOPTS_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((RADSURF+THERMAL)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VFOPTS_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFOPTS_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VFOPTS_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFQUERY) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (AUX12*PR_CFD*PR_THERM*(RADSURF+THERMAL)*CFD)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Query"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VFQUERY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFQUERY_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VFQUERY_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFQUERY_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VFQUERY_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFRC_01) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VFRC_01__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFRC_02) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VFRC_02__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFRC_03) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VFRC_03__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFRC_04) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VFRC_04__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VFRC_Q) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VFRC_Q__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VGEN2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VGEN2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VGEN_move) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VGEN_move__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VGEN_move_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VGEN_move_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VGET) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Variable to Par"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VGET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VGLUE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VGLUE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VIMP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VIMP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VIMP_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VIMP_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VINP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VINP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VINV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VINV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VLIST_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VLIST_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VLSCAL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VLSCAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VLSCAL_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VLSCAL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Free"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VMESH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH6) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+meshtool)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "4 to 6 sided"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VMESH6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH6_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMESH6_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH6_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMESH6_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH6_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMESH6_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH6_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMESH6_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH6_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMESH6_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMESH_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMESH_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMESH_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMESH_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMESH_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMESH_m) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+meshtool)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Free"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VMESH_m__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMOR21) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMOR21__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMOR22) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VMOR22__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMOR23) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMOR23__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMORP1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VMORP1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VMORPH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+RESULTS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VMORPH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VOFFST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Along Normal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VOFFST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VOFFST_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Along Normal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VOFFST_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VOVLAP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VOVLAP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VPLOT_deg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Degen Volus"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VPLOT_deg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VPLO_calc) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VPLO_calc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VPLO_eval) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VPLO_eval__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VPTN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VPTN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VPUT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Parameter to Var"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VPUT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VROTAT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "About Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VROTAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VROTAT_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "About Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VROTAT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSBA_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volume by Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSBA_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSBA_div_op2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volume by Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSBA_div_op2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSBA_div_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volume by Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSBA_div_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSBV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSBV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSBV_igs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSBV_igs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSBV_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSBV_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSBV_opt_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSBV_opt_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSBW_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volu by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSBW_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSBW_div_igs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volu by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSBW_div_igs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSBW_div_op2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volu by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSBW_div_op2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSBW_div_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volu by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSBW_div_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSEL_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Also select volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSEL_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSEL_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reselect volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSEL_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSEL_S) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSEL_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSEL_U) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unselect volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSEL_U__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Of Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sweep"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSWEEP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEP_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSWEEP_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEP_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSWEEP_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEP_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSWEEP_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEP_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSWEEP_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEP_m) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+meshtool)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sweep"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSWEEP_m__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEPa) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Sweep"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSWEEPa__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEPb) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sweep"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSWEEPb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEPb1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSWEEPb1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEPb2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSWEEPb2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEPb3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSWEEPb3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEPb4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSWEEPb4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSWEEPb5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSWEEPb5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSYMM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VSYMM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VSYMM_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VSYMM_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VTRAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_VTRAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VTRAN_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VTRAN_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_VariView) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Variable Viewer"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_VariView__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WAVES) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reorder by List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WAVES__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WERASE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Erase Wave List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WERASE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WFRONT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Est. Wavefront"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WFRONT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WFRONT_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WFRONT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WMORE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Extend Wave List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WMORE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WPAV) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "XYZ Locations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_WPAV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WPAVE_000) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Global Origin"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WPAVE_000__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WPCSYS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Specified Coord Sys"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WPCSYS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WPCSYS_activ) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Active Coord Sys"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WPCSYS_activ__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WPLA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "XYZ Locations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_WPLA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WRFULL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((STATIC*TRNSIENT*BUCKLING*MODAL)+simploff)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL)+(pp)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Stop Solution"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WRFULL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WRFULL_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Stop Solution"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WRFULL_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WRFULL_b) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Stop Solution"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WRFULL_b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WRFULL_c) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Stop Solution"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WRFULL_c__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WRNMODCYC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WRNMODCYC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WSORT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reorder by XYZ"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WSORT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WSPRINGS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Import fr ADAMS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WSPRINGS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_WSTART) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Wave List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_WSTART__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Wave_List) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Wave Lists"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Wave_List__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Weld2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Weld2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Whatsnew) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Menu Name"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Whatsnew__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Whatsnew_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Menu Name"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Whatsnew_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Wire) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Wire"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Wire__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Wire0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Wire0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Write_Beam) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Save"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Write_Beam__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Write_aux12) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (AUX12)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (pr_cfd+cfd)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Write Matrix"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Write_aux12__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_XFAPPEND_lsm) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Append by LSM"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_XFAPPEND_lsm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_XFAPPEND_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Append by Pick"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_XFAPPEND_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_XFDATA_lsm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create Crack by LSM"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_XFDATA_lsm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_XFDATA_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Working Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_XFDATA_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_XFDELETE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_XFDELETE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_XFENRICH_ls2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create by LSM"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_XFENRICH_ls2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_XFENRICH_lsm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create by LSM"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_XFENRICH_lsm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_XFENRICH_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create by Pick"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_XFENRICH_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_XFENRICH_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Working Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_XFENRICH_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_XFLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_XFLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad42) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SFELC*SFALC*SFLLC*SFLC)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad42__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad50) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad50__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad62) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad62__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad7) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad72) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SFELC*SFALC*SFLLC*SFLC)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad72__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad8) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad82) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad82__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad9) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad9__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZLoad92) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZLoad92__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo42) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo42__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo50) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo50__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo62) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo62__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo7) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo72) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo72__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo8) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo82) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo82__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo9) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo9__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZReCo92) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ZReCo92__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ZeDiode) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Zener Diode"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ZeDiode__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_accept_rjct) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_accept_rjct__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_accept_rjct2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_accept_rjct2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_adDYNROT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete All"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_adDYNROT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_bothld) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Trans and Dynam"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_bothld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_chkelms) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_chkelms__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_dDYNROT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Individ"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_dDYNROT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_del_Coriolis) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Accel CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_del_Coriolis__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_dynld) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Dynamic relax"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_dynld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_etyp_UNDOC) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_etyp_UNDOC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_oldPLESOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (cfd*MULTIFLD)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_oldPLESOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_oldPLNSOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_oldPLNSOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_oldPRESOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LSDYNA*LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element Solution"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_oldPRESOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_oldPRNSOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Solution"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_oldPRNSOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_p26DataSet) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_p26DataSet__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_p26DefineVar) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Variables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_p26DefineVar__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_p26FileSet) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_p26FileSet__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_p26GrphSet) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Graph"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_p26GrphSet__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_p26ListSet) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_p26ListSet__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcedit_124) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Edit Real Cnst"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_rcedit_124__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcedit_124_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Edit Real Cnst"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcedit_124_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_00) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_00__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_10) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_10__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_106) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_106__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_107) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_107__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_108) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_108__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_109) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_109__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_11) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_11__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_110) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_110__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_111) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_111__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_115) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_115__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_116) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_116__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_117) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_117__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_118) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_118__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_119) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_119__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_12) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_12__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_120) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_120__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_121) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_121__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_122) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_122__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_123) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_123__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Resistor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Inductor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_10) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Vltg Cntrl VS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_10__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_11) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Curr Cntrl VS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_11__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_12) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Curr Cntrl CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_12__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Capacitor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_34a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_34a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_3_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "DC/AC Harmonic"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_3_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_3_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Sinusoidal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_3_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_3_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pulse"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_3_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_3_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Exponential"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_3_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_3_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Piecewise"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_3_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Stranded Coil"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Mass Cond 2D"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_7) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Mass Cond 3D"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_8) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Mut Inductor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_9) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Vltg Cntrl CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_9__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_124_xx) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_124_xx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_125) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_125__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_125_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Diod"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_125_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_125_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_125_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_126) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_126__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_126_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "EMT"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_126_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_126_00) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_126_00__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_126_01) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_126_01__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_127) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_127__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_128) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_128__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_129) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_129__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_13) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_13__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_130) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_130__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_131) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_131__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_132) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_132__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_136) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_136__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_138) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_138__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_139) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_139__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_14) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_14__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_141) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_141__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_142) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_142__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_143) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_143__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_144) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_144__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_145) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_145__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_146) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_146__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_147) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_147__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_148) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_148__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_15) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_15__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_150) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_150__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151c) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151c__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151d) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151i) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151i__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151j) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151j__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151k) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151k__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151l) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151l__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151m) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151m__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151x) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151x__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151y) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151y__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151yy) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151yy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_151z) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_151z__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152c) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152c__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152d) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152i) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152i__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152j) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152j__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152k) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152k__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152l) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152l__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152m) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152m__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152x) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152x__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152y) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152y__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152yy) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152yy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_152z) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_152z__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_153) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_153__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_154) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_154__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_155) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_155__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_156) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_156__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_157) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_157__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_158) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_158__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_159) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_159__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_16) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_16__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_160) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_160__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_161) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_161__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_161A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_161A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_161B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_161B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_161C) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_161C__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_161E) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_161E__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_161F) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY13*KEY15)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_161F__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_161G) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_161G__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_161H) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_161H__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_161H2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_161H2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_161I) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_161I__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_161x) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_161x__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_162) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_162__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163B1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163B1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163B2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163B2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163B3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163B3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163C) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY554)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163C__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163C1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163C1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163C2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163C2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163C3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163C3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163C3A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163C3A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163C4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163C4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163C5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163C5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_163x) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_163x__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_164) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_164__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_165) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_165__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_166) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_166__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_166A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_166A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_166B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_166B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_167) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_167__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_168) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_168__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_169) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 169"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_169__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_170) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 170"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_170__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_171) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_171__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_172) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_172__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_173) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_173__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_174) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_174__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_175) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_175__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_176) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_176__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_177) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_177__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_178) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_178__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_179) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 179"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_179__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_c) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_c__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_d) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_e) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_e__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_f) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_f__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_g) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_g__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_h) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_i) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_i__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_j) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_j__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_k) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_k__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_17X_x) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_17X_x__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_18) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_18__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_180) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_180__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_181) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_181__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_182) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_182__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_182A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_182A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_182B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_182B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_183) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_183__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_183A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_183A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_183B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_183B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_184) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_184__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_185) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_185__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_185A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_185A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_185B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_185B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_186) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_186__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_187) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_187__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_188) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_188__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_189) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_189__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_19) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_19__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_190) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_190__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_191) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_191__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_192) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_192__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_192A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_192A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_192B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_192B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_193) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_193__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_193A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_193A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_193B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_193B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_194) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_194__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_195) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_195__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_19A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_19A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_19B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_19B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_20) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_20__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_200) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_200__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_201) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_201__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_202) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_202__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_202A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_202A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_202B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_202B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_203) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_203__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_203A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_203A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_203B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_203B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_204) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_204__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_205) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_205__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_208) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_208__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_209) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_209__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_21) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_21__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_212) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_212__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_212A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_212A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_212B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_212B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_213) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_213__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_213A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_213A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_213B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_213B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_214) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_214__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_215) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_215__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_216) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_216__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_217) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_217__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_21A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_21A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_21B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_21B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_21C) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_21C__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_21D) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_21D__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_22) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_22__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_220) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_220__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_221) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_221__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_223) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_223__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_226) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_226__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_227) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_227__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_23) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_23__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_230) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_230__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_230A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_230A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_230B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_230B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_231) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_231__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_232) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_232__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_233) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_233__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_233A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_233A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_233B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_233B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_236) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_236__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_236B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_236B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_237) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_237__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_237B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_237B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_238) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_238__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_238A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_238A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_238B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_238B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_239) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_239__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_24) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 24"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_24__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_240) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_240__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_241) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_241__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_242) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_242__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_25) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_25__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_251) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_251__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_252) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_252__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_26) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_263) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_263__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_264) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_264__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_265) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_265__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_27) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 27"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_27__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_272) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_272__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_273) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_273__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_278) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_278__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_279) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_279__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_27A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 27"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_27A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_27B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 27"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_27B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_27C) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 27"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_27C__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_28) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_28__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_281) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_281__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_285) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_285__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_288) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_288__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_289) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_289__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_29) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_29__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_290) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_290__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_2A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_2A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_2B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_2B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_30) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_30__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_300) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_300__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_31) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_31__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_32) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_32__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_33) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_33__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_34) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_34__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_35) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_35__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_36) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_36__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_37) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_37__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_38) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_38__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_38A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_38A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_38B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_38B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_39) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_39__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_40) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_40__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_41) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_41__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_42) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_42__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_42A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_42A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_42B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_42B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_43) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_43__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_44) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 44"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_44__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_45) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_45__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_45A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_45A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_45B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_45B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_46) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_46__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_47) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_47__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_48) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_48__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_49) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_49__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_50) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_50__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_51) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_51__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_52) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_52__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_53) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_53__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_53A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_53A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_53A1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_53A1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_53B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_53B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_53B1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_53B1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_54) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_54__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_55) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_55__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_55A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_55A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_55B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_55B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_55C) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_55C__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_55D) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_55D__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_56) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_56__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_57) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_57__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_58) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_58__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_59) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_59__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_60) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_60__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_61) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_61__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_62) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_62__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_63) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_63__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_64) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_64__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_65) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_65__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_66) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_66__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_67) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_67__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_68) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_68__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_69) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_69__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_7) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_70) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_70__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_70A) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_70A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_70B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_70B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_71) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_71__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_72) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_72__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_73) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_73__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_74) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_74__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_75) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_75__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_77) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_77__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_77A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_77A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_77B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_77B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_78) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_78__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_79) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_79__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_8) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_80) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_80__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_81) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_81__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_82) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_82__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_82A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_82A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_82B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_82B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_83) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_83__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_84) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_84__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_86) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_86__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_87) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_87__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_88) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_88__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_89) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_89__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_9) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_9__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_90) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 90"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_90__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_91) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_91__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_92) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_92__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_93) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_93__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_94) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_94__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_95) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_95__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_95_a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_95_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_96) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_96__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_97) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_97__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_97A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_97A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_97A1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_97A1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_97B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_97B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_97B1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_97B1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_98) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_98__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_99) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_99__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_NA_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_NA_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_NA_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_NA_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_NA_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_NA_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_NA_f) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_NA_f__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_UNDOC) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_UNDOC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_rcon_UNDOC_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_rcon_UNDOC_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_res_load) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_res_load__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_res_time) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_res_time__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ss_Adv1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Advanced Opts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ss_Adv1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ss_Advopt) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Adv Opts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ss_Advopt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ss_MESH) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "SmrtSize Levels"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ss_MESH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ss_SIZE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (meshtool)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Basic"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ss_SIZE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ss_STAT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ss_STAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_tbd) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_tbd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_thaddtbl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_thaddtbl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_thld) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_thld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_thnewtbl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_thnewtbl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_thnewtbl2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_thnewtbl2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_thnewtbl3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_thnewtbl3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_tholdtable) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_tholdtable__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_thvalue) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_thvalue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_tranld) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Transient only"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_tranld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Men_-REZONE-) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Manual Rezoning"]
   lappend tags branch
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Men_-REZONE-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   set nodes "Fnc_RZN_Start Men_RZN_CreateZn Fnc_RZN_ReadFile Fnc_RZN_SplitElm Fnc_RZN_Finish Fnc_RZN_DONE Fnc_RZN_Solve"
   foreach node $nodes {
      set ignore 0
      if { [llength $node] > 1 } {
         foreach filterdata [lrange $node 1 end] {
            set type [lindex $filterdata 0]
            set filters [lindex $filterdata 1]
            foreach filt $filters {
               if { ![ans_getfilter $type [list $filt]] } {
                  set ignore 1
                  continue
               }
            }
         }
      }
      if {$ignore} { continue }
      if { ![catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu([lindex $node 0]) Men_-REZONE- [expr $key+1] $level} branch] && [llength $branch]} {
         if { [string match {} $branch] } {
            continue
         }
         lappend branchLeafs $branch
      } else {
         #puts "[info script] error $node = $branch"
      }
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Men_P1_-REZONE-) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Manual Rezoning"]
   lappend tags branch
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Men_P1_-REZONE-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   set nodes "Fnc_RZN_CreateCM"
   foreach node $nodes {
      set ignore 0
      if { [llength $node] > 1 } {
         foreach filterdata [lrange $node 1 end] {
            set type [lindex $filterdata 0]
            set filters [lindex $filterdata 1]
            foreach filt $filters {
               if { ![ans_getfilter $type [list $filt]] } {
                  set ignore 1
                  continue
               }
            }
         }
      }
      if {$ignore} { continue }
      if { ![catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu([lindex $node 0]) Men_P1_-REZONE- [expr $key+1] $level} branch] && [llength $branch]} {
         if { [string match {} $branch] } {
            continue
         }
         lappend branchLeafs $branch
      } else {
         #puts "[info script] error $node = $branch"
      }
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Men_RZN_CreateZn) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (REMESHRZ)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create Remesh Zone(s)"]
   lappend tags branch
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Men_RZN_CreateZn__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   set nodes "Fnc_RZN_SelCM Fnc_RZN_SelElem Fnc_RZN_SelNode Fnc_AREMESH Men_RZN_MeshCtrl Men_RZN_GeomCtrl Fnc_Mesh_tool"
   foreach node $nodes {
      set ignore 0
      if { [llength $node] > 1 } {
         foreach filterdata [lrange $node 1 end] {
            set type [lindex $filterdata 0]
            set filters [lindex $filterdata 1]
            foreach filt $filters {
               if { ![ans_getfilter $type [list $filt]] } {
                  set ignore 1
                  continue
               }
            }
         }
      }
      if {$ignore} { continue }
      if { ![catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu([lindex $node 0]) Men_RZN_CreateZn [expr $key+1] $level} branch] && [llength $branch]} {
         if { [string match {} $branch] } {
            continue
         }
         lappend branchLeafs $branch
      } else {
         #puts "[info script] error $node = $branch"
      }
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Men_RZN_GeomCtrl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Geometry Controls"]
   lappend tags branch
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Men_RZN_GeomCtrl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   set nodes "Fnc_RZN_LCCAT"
   foreach node $nodes {
      set ignore 0
      if { [llength $node] > 1 } {
         foreach filterdata [lrange $node 1 end] {
            set type [lindex $filterdata 0]
            set filters [lindex $filterdata 1]
            foreach filt $filters {
               if { ![ans_getfilter $type [list $filt]] } {
                  set ignore 1
                  continue
               }
            }
         }
      }
      if {$ignore} { continue }
      if { ![catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu([lindex $node 0]) Men_RZN_GeomCtrl [expr $key+1] $level} branch] && [llength $branch]} {
         if { [string match {} $branch] } {
            continue
         }
         lappend branchLeafs $branch
      } else {
         #puts "[info script] error $node = $branch"
      }
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Men_RZN_MeshCtrl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Mesh Controls"]
   lappend tags branch
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Men_RZN_MeshCtrl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   set nodes "Fnc_Mesh_tool Fnc_RZN_DESIZE Fnc_RZN_MshrOpts Men_RZN_MeshCtrl_-ElementShapeChecking-"
   foreach node $nodes {
      set ignore 0
      if { [llength $node] > 1 } {
         foreach filterdata [lrange $node 1 end] {
            set type [lindex $filterdata 0]
            set filters [lindex $filterdata 1]
            foreach filt $filters {
               if { ![ans_getfilter $type [list $filt]] } {
                  set ignore 1
                  continue
               }
            }
         }
      }
      if {$ignore} { continue }
      if { ![catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu([lindex $node 0]) Men_RZN_MeshCtrl [expr $key+1] $level} branch] && [llength $branch]} {
         if { [string match {} $branch] } {
            continue
         }
         lappend branchLeafs $branch
      } else {
         #puts "[info script] error $node = $branch"
      }
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Men_RZN_MeshCtrl_-ElementShapeChecking-) { top {key 0} {level -1} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element Shape Checking"]
   lappend tags branch
   lappend tags HEAD
   set icon {}
   lappend tags headlevel__$level
   set branchName [list Men_RZN_MeshCtrl_-ElementShapeChecking-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   set nodes "Fnc_Shpp_Check Fnc_SHPP_toggle"
   foreach node $nodes {
      set ignore 0
      if { [llength $node] > 1 } {
         foreach filterdata [lrange $node 1 end] {
            set type [lindex $filterdata 0]
            set filters [lindex $filterdata 1]
            foreach filt $filters {
               if { ![ans_getfilter $type [list $filt]] } {
                  set ignore 1
                  continue
               }
            }
         }
      }
      if {$ignore} { continue }
      if { ![catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu([lindex $node 0]) Men_RZN_MeshCtrl_-ElementShapeChecking- [expr $key+1] $level} branch] && [llength $branch]} {
         if { [string match {} $branch] } {
            continue
         }
         lappend branchLeafs $branch
      } else {
         #puts "[info script] error $node = $branch"
      }
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
