namespace eval ::euidl::uidlmenu::uidlmenuItems {
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*AFUN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Angular Units"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*AFUN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*ASK) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi *ASK"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*ASK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*ASK_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi-*ASK"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*ASK_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*ASK_HELP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi *ASK"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*ASK_HELP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*CREATE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Create Macro"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*CREATE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Get Scalar Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_active) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Current settings"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_active__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_area) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_area__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_cdsy) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Coord systems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_cdsy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_compchr) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Character Name of Nth Component"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_compchr__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_compsno) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Number of Defined Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_compsno__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_comptyp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Component Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_comptyp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_continfo) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Entity"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_continfo__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_contno) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Number of Contact Definitions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_contno__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_elem) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_elem__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_ent) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "For selected set"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_ent__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_etyp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element types"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_etyp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_graph) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Graphics data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_graph__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_kp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_kp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_line) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_line__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_mat) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Material props"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_mat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_more) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "More *GET Functions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_more__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_node) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_node__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_onelems) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Current settings"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_onelems__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_onnodes) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Current settings"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_onnodes__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_opt) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Design opt data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_p1_elem) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_p1_elem__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_p1_etab) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elem table data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_p1_etab__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_p1_gm) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Global measures"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_p1_gm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_p1_misc) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elem table misc"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_p1_misc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_p1_mode) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Modal results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_p1_mode__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_p1_node) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_p1_node__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_p1_othr) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Other operations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_p1_othr__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_p1_path) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Path operations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_p1_path__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_p1_ssum) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elem table sums"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_p1_ssum__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_p26) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Time-hist var's"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_p26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_parm) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Parameter data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_parm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_parmloc) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Parameter data location in parameter table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_parmloc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_partel) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element Type"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_partel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_partmat) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Material Number"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_partmat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_partno) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Number of Parts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_partno__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_partrl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Real Constant Set"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_partrl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_rad) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Radiosity data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_rad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_rcon) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Real constants"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_rcon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_runst) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Run time stats"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_runst__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_subcnam) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Subcomponent Name Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_subcnam__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_subcomp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Subcomponent Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_subcomp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_tb) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Data tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_tb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_topo1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Topo opt data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_topo1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_topo2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Topo opt data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_topo2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_topo3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Topo opt data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_topo3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_topo4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Topo opt data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_topo4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_topo5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Topo opt data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_topo5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_volu) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_volu__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_weldmax) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Maximum Spotweld Number"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_weldmax__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_weldmin) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Minimum Spotweld Number"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_weldmin__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_weldno) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Number of Spotwelds"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_weldno__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_weldnod) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Spotweld Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_weldnod__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*GET_weldnxt) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Next Spotweld Number"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*GET_weldnxt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*MFOURI) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Matrix Fourier"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*MFOURI__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*MFUN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Matrix Functions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*MFUN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*MOONEY) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Calculate Const"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*MOONEY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*MOONEY2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*MOONEY2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*MOONEY2a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*MOONEY2a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*MOPER) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Matrix Operations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*MOPER__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*STAT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Named Parameters"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*STAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*STAT_array) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Define/Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*STAT_array__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*TOPER) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Table Operations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*TOPER__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VFILL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Fill"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VFILL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VFILL_beta) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VFILL_beta__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VFILL_data) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VFILL_data__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VFILL_gamm) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VFILL_gamm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VFILL_gdis) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VFILL_gdis__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VFILL_ramp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VFILL_ramp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VFILL_rand) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VFILL_rand__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VFILL_tria) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VFILL_tria__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VFUN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Vector Functions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VFUN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Get Array Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET_area) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET_area__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET_cdsy) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Coord systems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET_cdsy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET_elem) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET_elem__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET_kp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET_kp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET_line) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET_line__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET_node) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET_node__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET_p1_eta) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elem table data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET_p1_eta__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET_p1_nod) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET_p1_nod__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET_rcon) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Real constants"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET_rcon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET_tb) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Data tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET_tb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VGET_volu) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VGET_volu__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VITRP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Vector Interpolate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VITRP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VOPER) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Vector Operations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VOPER__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Array Parameters"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VPLO_choice) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Graph"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VPLO_choice__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VPUT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Put Array Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VPUT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VPUT_etab) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VPUT_etab__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VPUT_node) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VPUT_node__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VREAD) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Read from File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VREAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VREAD1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Read from File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VREAD1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VREAD2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VREAD2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VSCFUN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Vector-Scalar Func"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VSCFUN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*VWRITE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Write to File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*VWRITE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_*V_settings) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Operation Settings"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_*V_settings__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/CLEAR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Clear & Start New"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/CLEAR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/CLEAR_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/CLEAR_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/COLOR_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Entity Colors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/COLOR_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/CWD) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Change Directory"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/CWD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/CYCEXPAND) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (cycgen)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyc Expansion"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/CYCEXPAND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/ColA_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/ColA_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/ColE_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/ColE_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/ColL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/ColL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/ColV_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/ColV_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/EXIT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Exit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/EXIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/EXIT_ANS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Exit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/EXIT_ANS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/EXIT_WB) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Exit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/EXIT_WB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/EXPAND1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Periodic/Cyclic Symmetry Expansion"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/EXPAND1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/EXPAND1A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/EXPAND1A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/EXPAND_axi) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/EXPAND_axi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/EXPAND_axi1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/EXPAND_axi1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/EXPAND_modal) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Modal Cyclic Symmetry"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/EXPAND_modal__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/EXPAND_user) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Expansion by values"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/EXPAND_user__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/FILNAM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Change Jobname"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/FILNAM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/FILNAM2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/FILNAM2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/GRAPHICS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/GRAPHICS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/GST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Grph Solu Track"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/GST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/MPLIB) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Library Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/MPLIB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/MPLIB_STATUS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lib Path Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/MPLIB_STATUS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/PSEARCH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (BEGIN)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Macro Search Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/PSEARCH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TITLE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Change Title"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TITLE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TRLCY_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TRLCY_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing (3D)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_a2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_a2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_a2_of) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_a2_of__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_a2_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing on Picked Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/TXTRE_a2_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_a_off) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing (3D)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_a_off__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_a_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing (3D)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_a_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_bmp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Bitmap Filename"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_bmp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_cm) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing (3D)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_cm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_cm2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing on Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_cm2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_e) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing (3D)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_e__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_e2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_e2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_e2_of) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_e2_of__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_e2_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing on Picked Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/TXTRE_e2_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_e_off) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing (3D)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_e_off__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_e_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing (3D)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_e_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_err) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_err__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_jpg) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "JPEG Filename"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_jpg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_load) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Load Bitmap File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_load__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_off) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing (3D)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_off__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_off2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_off2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_png) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "PNG Filename"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_png__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_unload) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unload Bitmap File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_unload__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_v) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing (3D)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_v2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_v2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_v2_of) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_v2_of__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_v2_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing on Picked Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/TXTRE_v2_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_v_off) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing (3D)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_v_off__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TXTRE_v_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Texturing (3D)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/TXTRE_v_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TrnA_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/TrnA_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TrnE_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/TrnE_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/TrnV_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/TrnV_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/XFRM_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_/XFRM_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/XFrmA_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/XFrmA_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/XFrmE_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/XFrmE_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/XFrmK_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/XFrmK_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/XFrmL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/XFrmL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/XFrmN_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/XFrmN_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/XFrmV_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/XFrmV_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_/ZOO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Zoom by picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_/ZOO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppCharDens-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Charge Density"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppCharDens-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppCharDens-_HEAD$top) Fnc_-AppCharDens- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppCharge-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Charge"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppCharge-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppCharge-_HEAD$top) Fnc_-AppCharge- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppCurrDens-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Curr Density"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppCurrDens-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppCurrDens-_HEAD$top) Fnc_-AppCurrDens- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppCurrSeg-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP+edgeflx)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Curr Segment"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppCurrSeg-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppCurrSeg-_HEAD$top) Fnc_-AppCurrSeg- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppCurrent-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+elecstat+vfed+circ+edgeelc+timeintp)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Current"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppCurrent-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppCurrent-_HEAD$top) Fnc_-AppCurrent- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppElecField-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Electric Field"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppElecField-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppElecField-_HEAD$top) Fnc_-AppElecField- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppImped_E-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Impedance"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppImped_E-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppImped_E-_HEAD$top) Fnc_-AppImped_E- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppInfinite-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+edgeflx*ELECSTAT)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Infinite Surf"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppInfinite-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppInfinite-_HEAD$top) Fnc_-AppInfinite- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppMCI-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((CIRC+VECTORP+ELEM3D+MAGNETIC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mag circ intf"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppMCI-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppMCI-_HEAD$top) Fnc_-AppMCI- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppMagField-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Magnetic Field"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppMagField-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppMagField-_HEAD$top) Fnc_-AppMagField- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppMagFlux-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+SCALARP+edgeflx)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Magnetic Flux"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppMagFlux-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppMagFlux-_HEAD$top) Fnc_-AppMagFlux- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppMaxwell-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((MAGNETIC*EDGEFLX*ELECSTAT)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Maxwell Surf"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppMaxwell-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppMaxwell-_HEAD$top) Fnc_-AppMaxwell- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppShield-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Shield"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppShield-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppShield-_HEAD$top) Fnc_-AppShield- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppSurfChar-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Surf Chrg Den"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppSurfChar-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppSurfChar-_HEAD$top) Fnc_-AppSurfChar- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppVirtDisp-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((harmonic*ELEM2D+MAGNETIC*EDGEFLX)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Virtual Disp"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppVirtDisp-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppVirtDisp-_HEAD$top) Fnc_-AppVirtDisp- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppVoltDrop-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (VFED*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Voltage Drop"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-AppVoltDrop-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-AppVoltDrop-_HEAD$top) Fnc_-AppVoltDrop- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-BasePSD-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PSDRANDM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Base PSD Excit"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-BasePSD-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-BasePSD-_HEAD$top) Fnc_-BasePSD- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-BodyForces-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Body Forces"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-BodyForces-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-BodyForces-_HEAD$top) Fnc_-BodyForces- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-ConcPlot-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+STRUCTRL+pmethod)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Concrete Plot"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-ConcPlot-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-ConcPlot-_HEAD$top) Fnc_-ConcPlot- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-DelCurrDens-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Curr Density"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-DelCurrDens-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-DelCurrDens-_HEAD$top) Fnc_-DelCurrDens- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-DelElecField-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Electric field"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-DelElecField-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-DelElecField-_HEAD$top) Fnc_-DelElecField- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-DelExtPort-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Exterior Port"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-DelExtPort-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-DelExtPort-_HEAD$top) Fnc_-DelExtPort- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-DelIntPort-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Interior Port"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-DelIntPort-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-DelIntPort-_HEAD$top) Fnc_-DelIntPort- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-DelMagField-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Magnetic field"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-DelMagField-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-DelMagField-_HEAD$top) Fnc_-DelMagField- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-EdgeEDOF-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Edge DOF"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-EdgeEDOF-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-EdgeEDOF-_HEAD$top) Fnc_-EdgeEDOF- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-EdgeMVP-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(EDGEFLX*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Edge DOF"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-EdgeMVP-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-EdgeMVP-_HEAD$top) Fnc_-EdgeMVP- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Electric-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+vfed)*CIRC*TIMEINTP*EDGEELC*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Electric"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-Electric-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Electric-_HEAD$top) Fnc_-Electric- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Fluence-) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Fluence"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-Fluence-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Fluence-_HEAD$top) Fnc_-Fluence- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-FluidANSYS-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna*PR_FLUID)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (FLUID*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Fluid/ANSYS"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-FluidANSYS-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-FluidANSYS-_HEAD$top) Fnc_-FluidANSYS- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-FluidCFD-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna*PR_CFD)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Fluid/CFD"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-FluidCFD-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-FluidCFD-_HEAD$top) Fnc_-FluidCFD- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-ImprCurr-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((ELECTRIC+elecstat+circ+edgeelc+TIMEINTP)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Impressed Curr"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-ImprCurr-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-ImprCurr-_HEAD$top) Fnc_-ImprCurr- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-InitialCon-) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Initial Cond'n"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-InitialCon-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-InitialCon-_HEAD$top) Fnc_-InitialCon- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-J-Normal-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "J-Normal"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-J-Normal-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-J-Normal-_HEAD$top) Fnc_-J-Normal- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-J-Normal-T) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(elem3d+edgeelc+TIMEINTP)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "J-Normal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_-J-Normal-T__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Magnetic-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((MAGNETIC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Magnetic"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-Magnetic-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Magnetic-_HEAD$top) Fnc_-Magnetic- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-MultiPt-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (MULTIPNT*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Multi Point"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-MultiPt-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-MultiPt-_HEAD$top) Fnc_-MultiPt- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-MultiPtBas-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (MULTIPNT*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Multi Pt Base"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-MultiPtBas-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-MultiPtBas-_HEAD$top) Fnc_-MultiPtBas- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-MultiPtNod-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (MULTIPNT*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Multi Pt Nodal"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-MultiPtNod-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-MultiPtNod-_HEAD$top) Fnc_-MultiPtNod- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-NodePSD-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PSDRANDM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Node PSD Excit"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-NodePSD-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-NodePSD-_HEAD$top) Fnc_-NodePSD- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Options) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SCALARP*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Options Only"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_-Options__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-PMLOpt-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "PML Options"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-PMLOpt-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-PMLOpt-_HEAD$top) Fnc_-PMLOpt- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-PMLParamDel-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "PML Options"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-PMLParamDel-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-PMLParamDel-_HEAD$top) Fnc_-PMLParamDel- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-PSD-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PSDRANDM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "PSD"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-PSD-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-PSD-_HEAD$top) Fnc_-PSD- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-PerfEC-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (((PREP7*SOLUTION)+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Electric Wall"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-PerfEC-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-PerfEC-_HEAD$top) Fnc_-PerfEC- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-RotateNode-) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Rotate Node CS"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-RotateNode-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-RotateNode-_HEAD$top) Fnc_-RotateNode- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-ScalFluxN-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SCALARP*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Flux Normal"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-ScalFluxN-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-ScalFluxN-_HEAD$top) Fnc_-ScalFluxN- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-ScalFluxP-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SCALARP*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Flux Par'l"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-ScalFluxP-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-ScalFluxP-_HEAD$top) Fnc_-ScalFluxP- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-ScalarPot-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((SCALARP+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Scalar Poten"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-ScalarPot-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-ScalarPot-_HEAD$top) Fnc_-ScalarPot- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-SinglPtFor-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SINGLEPT*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "SinglePt Force"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-SinglPtFor-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-SinglPtFor-_HEAD$top) Fnc_-SinglPtFor- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-SinglePt-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SINGLEPT*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Single Point"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-SinglePt-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-SinglePt-_HEAD$top) Fnc_-SinglePt- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Solve-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (pp*LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solve"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-Solve-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Solve-_HEAD$top) Fnc_-Solve- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-StaticMag-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (harmonic+trnsient)*(NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Static Analy"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-StaticMag-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-StaticMag-_HEAD$top) Fnc_-StaticMag- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Structural-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (STRUCTRL*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Structural"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-Structural-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Structural-_HEAD$top) Fnc_-Structural- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Superelem-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (cfd*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Superelements"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-Superelem-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Superelem-_HEAD$top) Fnc_-Superelem- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Temperature-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Temperature"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-Temperature-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Temperature-_HEAD$top) Fnc_-Temperature- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Thermal-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (THERMAL*FLUID*CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Thermal"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-Thermal-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Thermal-_HEAD$top) Fnc_-Thermal- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-TimeInt-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+TIMEINTP+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "TimeIntegrated"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-TimeInt-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-TimeInt-_HEAD$top) Fnc_-TimeInt- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-VectorPot-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((VECTORP+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Vector Poten"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-VectorPot-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-VectorPot-_HEAD$top) Fnc_-VectorPot- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Voltage-) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Voltage"]
   lappend tags branch
   lappend tags HEAD
   lappend tags headlevel__$level
   set icon {}
   set branchName [list Fnc_-Voltage-__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }

   if { [catch {::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_-Voltage-_HEAD$top) Fnc_-Voltage- 0 $level} branchLeafs] } {
      set branchLeafs [list]
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_A) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Through KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AADD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AADD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AATT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AATT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AATT_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AATT_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AATT_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AATT_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ABBRESU) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Restore Abbr"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ABBRESU__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ABBSAVE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Save Abbr"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ABBSAVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ABEXTRACT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION+TFILM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "RayleighDamp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ABEXTRACT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ABEXTRACT2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ABEXTRACT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ABS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Absolute Value"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ABS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ACCAT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ACCAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ACCAT_del) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ACCAT_del__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ACCAT_m) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ACCAT_m__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ACEL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PR_STRUC*PR_FLUID*PR_CFD*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*FLUID*CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ACEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ACEL_dele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PR_STRUC*PR_FLUID*PR_CFD*pr_set) + lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*FLUID*CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Gravity"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ACEL_dele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ACLEAR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ACLEAR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ACNTRL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area Cntrls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ACNTRL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ADAMSOUT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Export to ADAMS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ADAMSOUT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ADAMSOUT1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Export to ADAMS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ADAMSOUT1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ADAPT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((BEGIN*SOLUTION)+(STRUCTRL*THERMAL*NOFILTER)+cfd+pmethod)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL)+(pp)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Adaptive Mesh"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ADAPT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ADD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ADD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ADELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas Only"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ADELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ADELE_sweep) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area and Below"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ADELE_sweep__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ADGL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List Degen Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ADGL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ADRAG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Along Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ADRAG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AESIZE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AESIZE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AESIZE_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AESIZE_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AESIZE_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AESIZE_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AESIZE_pdel) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clr Size"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AESIZE_pdel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AFILLT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area Fillet"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AFILLT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AFILLT_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area Fillet"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AFILLT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AFILLT_3d) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volume Fillet"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AFILLT_3d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AFILLT_3d_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volume Fillet"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AFILLT_3d_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AFLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Database Summary"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AFLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AFLIST_no) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AFLIST_no__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AFLIST_yes) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AFLIST_yes__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AGEN2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AGEN2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AGEN_move) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AGEN_move__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AGEN_move_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AGEN_move_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AGLUE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AGLUE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AINA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AINA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AINP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AINP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AINV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area with Volume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AINV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ALIST_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ALIST_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ALLSEL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ALLSEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMAP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Corners"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AMAP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMAP_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMAP_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMAP_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMAP_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMAP_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMAP_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMAP_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMAP_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Free"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AMESH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+meshtool)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "3 or 4 sided"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AMESH4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH4_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESH4_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH4_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESH4_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH4_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESH4_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH4_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESH4_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH4_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESH4_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESH_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESH_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESH_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESH_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESH_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESH_m) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+meshtool)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Free"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AMESH_m__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESHtar) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+meshtool)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Target Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AMESHtar__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESHtar_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESHtar_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESHtar_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESHtar_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESHtar_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESHtar_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESHtar_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESHtar_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMESHtar_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMESHtar_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMOR21) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMOR21__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMOR22) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AMOR22__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMOR23) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMOR23__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMORP1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AMORP1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AMORPH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+RESULTS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AMORPH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANORM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area Normals"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ANORM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANORM2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANORM2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANSYS_1st) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Fnc_ANSYS_1st"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANSYS_1st__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANTDIR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Direct Gain"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANTDIR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANTDIR_L) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Direct Gain"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANTDIR_L__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANTEFF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Efficiency"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANTEFF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANTPAT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pattern"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANTPAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANTPAT_L) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pattern"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANTPAT_L__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANTPGAIN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Power Gain"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANTPGAIN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANTPRAD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Rad Power"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANTPRAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANTYPE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "New Analysis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANTYPE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANTYPE_01) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "New Analysis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANTYPE_01__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ANTYPE_02) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "New Analysis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ANTYPE_02__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AOFFST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Offset"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AOFFST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AOFFST_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Offset"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AOFFST_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AOVLAP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AOVLAP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_APLOT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_APLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_APLOT_deg) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Degen Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_APLOT_deg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_APPEND) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Append Selected"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_APPEND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_APPEND_l) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_APPEND_l__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_APPEND_ll) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_APPEND_ll__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_APPEND_ln) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_APPEND_ln__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_APPEND_n) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_APPEND_n__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_APPEND_nl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_APPEND_nl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_APPEND_nn) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_APPEND_nn__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_APTN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_APTN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ARC_opts) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION+STATIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Arc-Length Opts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ARC_opts__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AREF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AREF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AREF_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Refine Mesh at AREA"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AREF_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AREF_2a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AREF_2a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AREF_2b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AREF_2b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AREMESH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create Rezone Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AREMESH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AREVERSE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "of Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AREVERSE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AREVERSE2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AREVERSE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AROTAT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "About Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_AROTAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AROTAT_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "About Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AROTAT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ARRAY_ADD) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Add New Array"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ARRAY_ADD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ARSCAL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ARSCAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ARSCAL_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ARSCAL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ARSYM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ARSYM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ARSYM_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ARSYM_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBA_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBA_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBA_div_op2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ASBA_div_op2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBA_div_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBA_div_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBA_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBA_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBA_opt_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ASBA_opt_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBL_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBL_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBL_div_op2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ASBL_div_op2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBL_div_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBL_div_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBV_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by Volume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBV_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBV_div_igs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by Volu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBV_div_igs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBV_div_op2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by Volume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ASBV_div_op2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBV_div_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by Volume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBV_div_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBW_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBW_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBW_div_igs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBW_div_igs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBW_div_op2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ASBW_div_op2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASBW_div_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASBW_div_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASELH_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Also select areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASELH_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASELH_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reselect areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASELH_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASELH_S) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASELH_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASELH_U) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unselect areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASELH_U__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASEL_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Also select areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASEL_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASEL_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reselect areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASEL_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASEL_S) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASEL_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASEL_U) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unselect areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASEL_U__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASKIN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Skinning"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASKIN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASUB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Overlaid on Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ASUB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ASUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Of Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ASUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ATAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST26+HARMONIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Arctangent"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ATAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ATRAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ATRAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ATRAN_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ATRAN_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*(SOLUTION+lstepgt1+(pr_cfd+cfd*PR_STRUC*PR_FLUID*PR_THERM)))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (expass)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Analysis Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_2a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_2a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_2a2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_2a2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_2a3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_2a3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_2a4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_2a4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_2a5) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_2a5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_2a5a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_2a5a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_2a6) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_2a6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_2a7) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_2a7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_2a7a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_2a7a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_3a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_3a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_3a2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_3a2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_3a3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_3a3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_4a0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_4a0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_4a1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_4a1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_4a1a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_4a1a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_4a1r) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_4a1r__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_4a3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_4a3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_4x) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_4x__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_7) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Opts_8) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Opts_8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_An_Sub_CMS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_An_Sub_CMS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_App_Coriolis) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Set Accel CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_App_Coriolis__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_App_Stress) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION*PREP7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Import Stress"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_App_Stress__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ArclenWarn) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ArclenWarn__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Aux12Fil) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list aux12]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Radiation Opts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Aux12Fil__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AxisAdd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AxisAdd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AxisAddEdit2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add / Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AxisAddEdit2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AxisAddEdit3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AxisAddEdit3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_AxisEdit) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_AxisEdit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFADELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFADELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFADELE_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFADELE_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFADELE_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFADELE_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFADELE_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFADELE_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFADELE_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFADELE_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFADELE_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFADELE_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFADELE_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFADELE_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFADELE_port) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFADELE_port__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFADELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFADELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFADELE_vltg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (VFED*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFADELE_vltg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFADE_norm_v) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION+TFILM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFADE_norm_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFALIS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFALIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFA_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_chrg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFA_chrg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFA_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_ef2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFA_ef2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFA_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_flue2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFA_flue2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFA_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_h2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFA_h2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFA_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFA_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_js2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFA_js2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_js3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFA_js3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_norm_v) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION+TFILM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFA_norm_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_norm_v1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFA_norm_v1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFA_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFA_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_vltg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (VFED*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFA_vltg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFA_vltg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFA_vltg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFCUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Body Ld"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFCUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_Force) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDELE_Force__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDELE_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_dyna) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Temps"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFDELE_dyna__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDELE_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDELE_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDELE_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDELE_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDELE_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_mvdi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (MAGNETIC*EDGEFLX*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDELE_mvdi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_port) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDELE_port__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_teCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Node Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDELE_teCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDEL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFDEL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFDEL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFDEL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFECUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elem Body Lds"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFECUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_FVIN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDELE_FVIN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFEDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDELE_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDELE_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDELE_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDELE_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDELE_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDELE_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDELE_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFEDELE_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_teCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Element Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDELE_teCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDELE_vltg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (VFED*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDELE_vltg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFEDE_norm_v) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION+TFILM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFEDE_norm_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFELIS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFELIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFESCAL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elem Body Lds"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFESCAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_EF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+ELEM3D)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_EF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_EF2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_EF2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_FVI2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_FVI2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_FVIN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_FVIN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_chrgd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_chrgd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_chrgd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_chrgd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_ef2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_ef2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_flue2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_flue2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_h2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_h2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_js2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_js2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_norm_v) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION+TFILM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_norm_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_norm_v1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_norm_v1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_temp2CM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_temp2CM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_tempCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Element Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_tempCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_vltg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (VFED*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFE_vltg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFE_vltg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFE_vltg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFINT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Interp Body Forc"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFINT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFKDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKDELE_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFKDELE_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKDELE_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFKDELE_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKDELE_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFKDELE_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKDELE_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFKDELE_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKDELE_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*THERMAL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFKDELE_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKDELE_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFKDELE_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKDELE_mvdi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (MAGNETIC*EDGEFLX*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFKDELE_mvdi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFKDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKDEL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Keyoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFKDEL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKDEL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFKDEL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFKLIS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFKLIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_chrgd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFK_chrgd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_chrgd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFK_chrgd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFK_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_ef2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFK_ef2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFK_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_flue2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFK_flue2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFK_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_h2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFK_h2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*THERMAL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFK_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFK_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_js2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFK_js2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_mvdi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((harmonic*ELEM2D+MAGNETIC*EDGEFLX)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFK_mvdi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_mvdi2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFK_mvdi2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFK_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFK_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFK_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFK_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFK_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFLDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFLDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFLDELE_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFLDELE_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFLDELE_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFLDELE_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFLDELE_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFLDELE_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFLDELE_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFLDELE_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFLDELE_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*THERMAL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFLDELE_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFLDELE_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFLDELE_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFLDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFLDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFLIST_dyna) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Temps"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFLIST_dyna__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFLLIS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFLLIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFL_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_chrg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFL_chrg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFL_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_ef2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFL_ef2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFL_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_flue2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFL_flue2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFL_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_h2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFL_h2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*THERMAL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFL_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFL_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_js2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFL_js2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_js3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFL_js3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFL_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFL_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFL_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFSCALE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Body Ld"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFSCALE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFTRAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Body Loads"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFTRAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFUNIF_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Uniform Fluen"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFUNIF_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFUNIF_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*THERMAL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Uniform Heat Gen"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFUNIF_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFUNIF_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Unif Nodal Temp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFUNIF_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFVDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Volms"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFVDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFVDELE_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFVDELE_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFVDELE_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFVDELE_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFVDELE_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFVDELE_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFVDELE_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFVDELE_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFVDELE_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFVDELE_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFVDELE_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFVDELE_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFVDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFVDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFVDELE_vltg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (VFED*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFVDELE_vltg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFVLIS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFVLIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFV_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_chrg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFV_chrg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFV_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_ef2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFV_ef2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFV_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_flue2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFV_flue2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFV_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_h2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFV_h2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFV_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFV_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_js2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFV_js2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFV_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFV_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_vltg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (VFED*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BFV_vltg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BFV_vltg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BFV_vltg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_Force) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BF_Force__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_chrgd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BF_chrgd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_chrgd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_chrgd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_ef) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BF_ef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_ef2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_ef2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_flue) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BF_flue__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_flue2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_flue2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_h) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BF_h__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_h2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_h2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BF_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP*EDGEFLX*EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BF_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_js2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_js2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_js3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_js3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_mvdi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((harmonic*ELEM2D+MAGNETIC*EDGEFLX)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BF_mvdi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_mvdi2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_mvdi2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BF_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_tem3a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_tem3a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_tem3b2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_tem3b2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_tem3b3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_tem3b3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BF_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_temp2CM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BF_temp2CM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BF_tempCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Node Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BF_tempCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BIOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeflx+SCALARP)*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Biot-Savart"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BIOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BLC4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By 2 Corners & Z"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BLC4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BLC4_2d) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By 2 Corners"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BLC4_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BLC5) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Centr,Cornr,Z"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BLC5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BLC5_2d) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Centr & Cornr"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BLC5_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BLC5_iges) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Block"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BLC5_iges__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BLOCK) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Dimensions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BLOCK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BSPLIN_kp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Spline thru KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BSPLIN_kp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BSPLIN_kp_end) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BSPLIN_kp_end__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BSPLIN_kp_op) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Spline thru KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BSPLIN_kp_op__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BSPLIN_wp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Spline thru Locs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BSPLIN_wp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BSPLIN_wp_end) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BSPLIN_wp_end__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BSPLIN_wp_op) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Spline thru Locs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_BSPLIN_wp_op__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BeamComposit) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Composite Sections"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BeamComposit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_BeamPreInt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "NL Generalized"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_BeamPreInt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Beam_Edit) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Edit / Built-up"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Beam_Edit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Beam_Tool) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Common Sections"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Beam_Tool__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Bool_opts) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Settings"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Bool_opts__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CAPMATR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECSTAT)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Capac Matrix"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CAPMATR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CAPMATR_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CAPMATR_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CBDOF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Interpolate DOF"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CBDOF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CDREAD) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Read"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CDREAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CDWRITE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Write"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CDWRITE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7)*(NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Constraint Eqn"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CECMOD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)*(NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Modify ConstrEqn"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CECMOD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CEDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7)*(NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Del Constr Eqn"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CEDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CEINTF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7)*(NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Adjacent Regions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CEINTF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CENTER) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "At Curvature Ctr"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CENTER__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CENTER2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "At Curvature Ctr"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CENTER2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CERIG_P) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7)*(NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Rigid Region"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CERIG_P__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CERIG_P2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7)*(NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Rigid Region"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CERIG_P2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CESGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Gen w/Same DOF"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CESGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CFACT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST26+HARMONIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Complx ScaleFact"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CFACT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CFDLoad) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CFDLoad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CFDLoad2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CFDLoad2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CFDLoad4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CFDLoad4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CFDLoad50) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CFDLoad50__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CFDLoad6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CFDLoad6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CFDLoad7) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CFDLoad7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CFDLoad8) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CFDLoad8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CFDLoad9) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CFDLoad9__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CFX_1st) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Fnc_CFX_1st"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CFX_1st__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CHECK_ESEL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+begin)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Select Warning/Error Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CHECK_ESEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CHECK_plt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+begin)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Warning/Error Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CHECK_plt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CHKMODCYC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CYCGEN+(MODAL+(PR_STRUC*STRUCTRL))))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Modal Cyclic Sym"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CHKMODCYC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CHK_ROUT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CHK_ROUT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CIRCLE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Full Circle"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CIRCLE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CIRCLE_arc) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Cent & Radius"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CIRCLE_arc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CIRCLE_arc2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CIRCLE_arc2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CLOG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Common Log"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CLOG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Create Component"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMACEL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PR_STRUC*PR_FLUID*PR_CFD*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*FLUID*CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMACEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDELE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_AK) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMDOMEGA_AK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_AKPik) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Kpts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMDOMEGA_AKPik__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_AN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMDOMEGA_AN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_ANPik) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMDOMEGA_ANPik__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_AxK) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMDOMEGA_AxK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_AxN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMDOMEGA_AxN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_Axis) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMDOMEGA_Axis__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_DK) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMDOMEGA_DK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_DKPik) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Kpt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMDOMEGA_DKPik__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_DN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMDOMEGA_DN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_DNPik) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMDOMEGA_DNPik__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_Dir) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By origin"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMDOMEGA_Dir__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMDOMEGA_dele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Component"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMDOMEGA_dele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMEDIT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Edit Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMEDIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMGRP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Create Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMGRP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMLIST1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMLIST1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMLIST2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMLIST2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMLIST3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMLIST3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMLIST4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMLIST4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_AK) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMOMEGA_AK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_AKPik) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Kpts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMOMEGA_AKPik__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_AN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMOMEGA_AN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_ANPik) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMOMEGA_ANPik__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_AxK) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMOMEGA_AxK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_AxN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMOMEGA_AxN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_Axis) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMOMEGA_Axis__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_DK) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMOMEGA_DK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_DKPik) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Kpt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMOMEGA_DKPik__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_DN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMOMEGA_DN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_DNPik) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMOMEGA_DNPik__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_Dir) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By origin"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMOMEGA_Dir__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMOMEGA_dele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Component"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMOMEGA_dele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMPL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By Name / Set Number"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMPL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMPL1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMPL1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMPL2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMPL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMPL3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMPL3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMSEL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMSEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMSEL1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMSEL1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMSEL2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select Comp/Assembly"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMSEL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMSE_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Also select components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMSE_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMSE_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reselect components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMSE_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMSE_S) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMSE_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMSE_U) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unselect components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CMSE_U__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMS_Name) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Define Name"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMS_Name__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CMS_Stat) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "CMS Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CMS_Stat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CNVTOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence Crit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CNVTOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CNVTOL2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CNVTOL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CNVTOL_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence Crit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CNVTOL_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CNVTOL_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Static"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CNVTOL_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CNVTOL_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Harmonic"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CNVTOL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CNVTOL_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Transient"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CNVTOL_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CNVTOL_ADD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence Crit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CNVTOL_ADD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CNVTOL_ADD0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CNVTOL_ADD0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CNVTOL_EDIT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence Crit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CNVTOL_EDIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CON4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CON4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CON4_iges) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cone"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CON4_iges__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CONE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Dimensions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CONE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CONJUG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST26+HARMONIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Complex Conjugat"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CONJUG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_COPSD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((POST26+PSDRANDM)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Calc Covariance"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_COPSD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_COVAL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cospectral"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_COVAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_COVAL1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_COVAL1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_COVAL2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_COVAL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_COVAL3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_COVAL3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_COVAL4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_COVAL4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CO_QD_eras) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PSDRANDM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Erase Co & Quad"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CO_QD_eras__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Couple DOFs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CPCYC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Offset Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CPCYC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CPDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Del Coupled Sets"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CPDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CPINTF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Coincident Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CPINTF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CPLGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Gen w/Same Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CPLGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CPSGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Gen w/Same DOF"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CPSGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CP_1a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cupl DOFs w/Mstr"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CP_1a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CP_1b) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Couple DOFs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CP_1b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CP_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Couple DOFs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CP_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CRPLIM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Creep Criterion"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CRPLIM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By 3 Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CS2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CS2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSCIR) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Move Singularity"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CSCIR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSDELE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Local CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CSDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSKP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "By 3 Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CSKP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSKP2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CSKP2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSWPLA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "At WP Origin"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CSWPLA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSYS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Specified Coord Sys"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CSYS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSecAddEdit2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add / Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CSecAddEdit2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSecAddEdit3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CSecAddEdit3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSecBolt) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CSecBolt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSecCircle) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CSecCircle__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSecCylind) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CSecCylind__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CSecSphere) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CSecSphere__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CURR2D) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+VECTORP+ELEM2D+elem3d)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((edgeflx+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Current"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CURR2D__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CUTCONTROL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (NLCNTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cutback Control"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CUTCONTROL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CUTCONTROL_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (NLCNTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cutback Control"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CUTCONTROL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_CntcArFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Contact Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_CntcArFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_CntcKpFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Contact Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_CntcKpFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_CntcLn3DFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Contact Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_CntcLn3DFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_CntcLnFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Contact Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_CntcLnFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_CntcNdFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Surface Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_CntcNdFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_CntcVlFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Contact Bodies"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_CntcVlFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_CntcVolFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Contact Bodies"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_CntcVolFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_DelePair) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (CNTCELEM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Pair"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CWz_DelePair__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_Entry) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Pair"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CWz_Entry__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_EntryOld) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((nolnmesh|noareamesh)|noelems)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Wizard"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CWz_EntryOld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_FlipArea) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (CNTCELEM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "of Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_FlipArea__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_FlipElem) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (CNTCELEM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "of Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_FlipElem__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_FlipLine) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (CNTCELEM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "of Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_FlipLine__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_NodePick) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_NodePick__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_PiloKP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pilot Node Definition"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_PiloKP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_PiloND) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pilot Node Definition"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_PiloND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_PiloWP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pilot Node Definition"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_PiloWP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_RevNrmLb) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (CNTCELEM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reverse Normal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CWz_RevNrmLb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_TargArFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Target Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_TargArFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_TargArRgd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Target Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_TargArRgd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_TargLnFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Target Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_TargLnFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_TargLnRgd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Through KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_TargLnRgd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_TargNdFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Target Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_TargNdFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_TargVlFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Target Body"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_TargVlFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_TargVlRgd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Target Body (Area)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_TargVlRgd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_TargVolFlx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Target Body"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_TargVolFlx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_TargVolRgd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Target Body"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CWz_TargVolRgd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CWz_ViewPair) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (CNTCELEM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "View and Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CWz_ViewPair__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCEXPAND_01) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyc Expansion"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCEXPAND_01__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCEXPAND_05) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyc Expansion"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCEXPAND_05__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCEXPAND_06) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyc Expansion"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCEXPAND_06__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCEXPAND_07) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyc Expansion"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCEXPAND_07__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCEXPAND_08) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyc Expansion"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCEXPAND_08__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (((PREP7)+(igesfail))+cyclic)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Sector"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCLICAUTO) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (((PREP7)+(igesfail))+cycgen)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Auto Defined"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCLICAUTO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCLICMAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (((PREP7)+(igesfail))+cycgen)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "User Defined"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCLICMAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCLICOFF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (((PREP7)+(igesfail))+cycgen)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reset (OFF)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCLICOFF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCLICSTAT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (((PREP7)+(igesfail))+cycgen)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCLICSTAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCLICUNDO) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7)+(igesfail))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Del Dupl Sector"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCLICUNDO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCLIC_exec) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Execute cyclic"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCLIC_exec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*(SOLUTION+CYCLIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (CYCLIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT_01) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT_01__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT_02) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT_02__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT_03) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT_03__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT_04) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT_04__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT_05) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT_05__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT_06) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT_06__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT_07) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT_07__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT_08) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT_08__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT_09) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT_09__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT_10) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT_10__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCOPT_11) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCOPT_11__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYCSOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Modal Cyclic Sym"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYCSOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYL4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solid Cylinder"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CYL4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYL4_2d) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solid Circle"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CYL4_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYL4_holl_2d) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Annulus"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CYL4_holl_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYL4_hollow) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hollow Cylinder"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CYL4_hollow__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYL4_iges) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solid Cylindr"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CYL4_iges__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYL4_part) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Partial Cylinder"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CYL4_part__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYL4_part_2d) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Partial Annulus"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CYL4_part_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYL5) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By End Pts & Z"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CYL5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYL5_2d) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By End Points"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CYL5_2d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CYLIND) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Dimensions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CYLIND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CheckCyc) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CheckCyc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden (Change Element Color)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_MA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_MA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_R0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_R0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_R10) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_R10__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_R11) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_R11__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_R12) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_R12__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_R4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_R4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_R5) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_R5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_R6) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_R6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_R7) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_R7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_R8) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_R8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_R9) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_R9__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirBD_SF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirBD_SF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Resistor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Inductor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP10_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Vltg Cntl VS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP10_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP10_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Vltg Cntl VS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP10_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP11_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Curr Cntl VS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP11_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP11_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Curr Cntl VS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP11_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP11_p3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirP11_p3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP12_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Curr Cntl CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP12_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP12_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Curr Cntl CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP12_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP12_p3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirP12_p3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Capacitor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP3_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Constant Amplitude"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP3_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP3_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sinusoidal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP3_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP3_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pulse"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP3_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP3_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Exponential"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP3_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP3_4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Piecewise"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP3_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP4_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Constant Amplitude"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP4_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP4_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sinusoidal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP4_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP4_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pulse"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP4_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP4_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Exponential"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP4_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP4_4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Piecewise"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP4_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP5_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Strnd Coil"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP5_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP5_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Strnd Coil"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP5_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP6_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mass Cond 2D"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP6_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP6_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mass Cond 2D"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP6_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP7_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mass Cond 3D"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP7_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP7_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mass Cond 3D"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP7_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP8) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mutual Ind"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP9_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Vltg Cntl CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP9_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirP9_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Vltg Cntl CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirP9_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirPR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirPR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirPWF_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Src Waveform"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_CirPWF_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirPWF_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirPWF_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirPW_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden (Error Trap)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirPW_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirPW_4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden (Error Trap)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirPW_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirPW_5) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden (Error Trap)"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirPW_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirPW_6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirPW_6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirPW_7) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirPW_7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirPW_8) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirPW_8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Resistor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Inductor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR10) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Vltg Cntrl VS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR10__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR11) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Curr Cntrl VS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR11__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR12) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Curr Cntrl CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR12__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Capacitor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR3_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Constant Amplitude"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR3_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR3_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sinusoidal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR3_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR3_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pulse"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR3_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR3_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Exponential"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR3_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR3_4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Piecewise"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR3_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR5) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Stranded Coil"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR6) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mass Cond 2D"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR7) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mass Cond 3D"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR8) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mut Inductor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CirR9) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Vltg Cntrl CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CirR9__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Cir_Merge) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Merge Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Cir_Merge__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Cir_Merge0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Cir_Merge0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Cir_Nod) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Cir_Nod__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Cir_WP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Set Grid"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Cir_WP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Cl_Mesh) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clear Radiation Surface Mesh"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Cl_Mesh__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Clear_Sym) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clear Symmetry"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Clear_Sym__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Cms_Opts) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Cms_Opts__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Cms_Opts_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Cms_Opts_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Cms_Opts_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Cms_Opts_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CommontoSect) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Write From Common"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CommontoSect__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Condensation) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Condensation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Condensation__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ContSectAdd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ContSectAdd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ContSectEdit) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ContSectEdit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CopyMatModel) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Copy Material Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CopyMatModel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CopyMatNo) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Copy Material Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CopyMatNo__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CopyMatYes) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Copy Material Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CopyMatYes__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Coriolis) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Coriolis Effects"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Coriolis__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Coriolis_del) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Coriolis Effects"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Coriolis_del__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Cr_Mesh) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Generate Radiation Surface Mesh"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Cr_Mesh__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CustBeam_Fin) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Finish"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CustBeam_Fin__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CycPhase_Clear) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Clear"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CycPhase_Clear__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CycPhase_Get) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Get"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CycPhase_Get__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CycPhase_List) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CycPhase_List__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CycPhase_Put) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Put Phase Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CycPhase_Put__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CycPhase_Status) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CycPhase_Status__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_CycPhase_Sweep) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Phase Sweep"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_CycPhase_Sweep__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Cyc_Sym) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyclic Symmetry"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Cyc_Sym__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DADELE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(VECTORP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DADELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_azP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(EDGEFLX*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE_azP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_mag) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(edgeflx+SCALARP)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE_mag__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_pec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (((PREP7*SOLUTION)+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE_pec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*TFILM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_spec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE_spec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_spec2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DADELE_spec2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*THERMAL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_ti) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(TIMEINTP+edgeelc)*NOFILTER))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE_ti__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_turb) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE_turb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_turb2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DADELE_turb2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_turb3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DADELE_turb3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_uva) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DADELE_uva__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_v) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_v2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DADELE_v2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_v3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DADELE_v3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADELE_volt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADELE_volt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADEL_flo) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DADEL_flo__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADEL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DADEL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DADEL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DADEL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DALIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DALIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeflx+VECTORP)*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_a2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DA_a2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_aN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((ELEM3D+VECTORP+edgeflx)*NOFILTER))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_aN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_aP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((ELEM3D+VECTORP+edgeflx)*NOFILTER))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_aP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_asym) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_asym__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_azN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(EDGEFLX*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Flux Normal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DA_azN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_azP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(EDGEFLX*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_azP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_flo) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_flo__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_mag) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeflx+SCALARP)*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_mag__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_mag2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DA_mag2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_magN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeflx+SCALARP)*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_magN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_pec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (((PREP7*SOLUTION)+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_pec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_pmc) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (((PREP7*SOLUTION)+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Magnetic Wall"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DA_pmc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*TFILM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_spec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_spec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_symm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_symm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*THERMAL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DA_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_turb) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_turb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_u) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_u__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_u2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DA_u2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_uCFD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_uCFD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_v) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_volt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_volt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_volt0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_volt0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_volt0_ti) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(elem3d+edgeelc+TIMEINTP)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_volt0_ti__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DA_volt_ti) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(TIMEINTP*NOFILTER)+edgeelc)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DA_volt_ti__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DCUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Constraints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DCUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDAM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (DDAM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "DDAM Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DDAM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(VECTORP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_azP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(EDGEFLX*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_azP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_mag) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SCALARP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_mag__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_pec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (((PREP7*SOLUTION)+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_pec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_spec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_spec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_spec2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DDELE_spec2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DDELE_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_ti) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeelc+TIMEINTP)*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_ti__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_turb) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_turb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_turb2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DDELE_turb2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_turb3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DDELE_turb3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_uva) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_uva__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_uva2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DDELE_uva2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_uva2CM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DDELE_uva2CM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_uvaCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Node Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_uvaCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_v) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_v2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DDELE_v2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_v3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DDELE_v3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDELE_volt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDELE_volt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDEL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DDEL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DDEL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DDEL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DELTIM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Time Step Size"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DELTIM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DERIV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Derivative"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DERIV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DESIZE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Other"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DESIZE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DESOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elem Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DESOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DESOL2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DESOL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DETAB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ElemTabl Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DETAB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DETAB2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DETAB2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DIST_KP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DIST_KP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DIST_KP_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DIST_KP_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DIST_ND) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DIST_ND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DIST_ND_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DIST_ND_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DJDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(ELMJOINT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Joint Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DJDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DJDE_joint) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(ELMJOINT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Joint Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DJDE_joint__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DJDE_joint2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DJDE_joint2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DJLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Element"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DJLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DJ_joint) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(ELMJOINT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Joint Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DJ_joint__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DJ_joint2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DJ_joint2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDELE_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(VECTORP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DKDELE_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DKDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDELE_ends) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DKDELE_ends__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDELE_enke) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DKDELE_enke__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDELE_mag) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SCALARP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DKDELE_mag__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDELE_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DKDELE_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DKDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDELE_ti) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(TIMEINTP+edgeelc)*NOFILTER))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DKDELE_ti__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDELE_uva) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DKDELE_uva__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDELE_uva2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DKDELE_uva2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDELE_volt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DKDELE_volt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDEL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DKDEL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKDEL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DKDEL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DKLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DKLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeflx+VECTORP)*NOFILTER))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_a2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DK_a2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_enke) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_enke__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_enke2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DK_enke2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_mag) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeflx+SCALARP)*NOFILTER))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_mag__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_mag2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DK_mag2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_mult_del) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(MULTIPNT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_mult_del__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_mult_del2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DK_mult_del2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_multi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(MULTIPNT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_multi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_multi2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DK_multi2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DK_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_psd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_psd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_psd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DK_psd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_psd_dele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_psd_dele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_psd_dele2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DK_psd_dele2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DK_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_u) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_u__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_u2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DK_u2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_uCFD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_uCFD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_v) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_volt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_volt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DK_volt_ti) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(TIMEINTP*NOFILTER)+edgeelc)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DK_volt_ti__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DLDELE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(VECTORP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLDELE_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DLDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_pec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (((PREP7*SOLUTION)+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLDELE_pec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*TFILM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLDELE_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_spec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLDELE_spec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_spec2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DLDELE_spec2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*THERMAL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_turb) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLDELE_turb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_turb2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DLDELE_turb2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_turb3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DLDELE_turb3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_uva) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DLDELE_uva__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_v) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLDELE_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_v2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DLDELE_v2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_v3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DLDELE_v3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDELE_volt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLDELE_volt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDEL_flo) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DLDEL_flo__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDEL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLDEL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLDEL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DLDEL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DLLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DLLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeflx+VECTORP)*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_a2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DL_a2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_aN_na) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((ELEM2D+VECTORP+edgeflx)*NOFILTER))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DL_aN_na__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_aP_na) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((ELEM2D+VECTORP+edgeflx)*NOFILTER))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_aP_na__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_asym) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "...with Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_asym__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_asym_na) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_asym_na__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_dof) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_dof__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_dof2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DL_dof2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_magN_na) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DL_magN_na__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_magP_na) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DL_magP_na__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_pec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (((PREP7*SOLUTION)+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_pec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*TFILM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_spec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_spec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_symm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "...with Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_symm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_symm_na) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_symm_na__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(CFD*THERMAL*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DL_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_turb) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_turb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_u) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_u__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_u2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DL_u2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_uCFD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_uCFD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_v) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_volt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_volt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DL_volt0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DL_volt0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DMPEXT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION+TFILM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Frequency Range"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DMPEXT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DMPEXT2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DMPEXT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DMPEXT4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DMPEXT4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DMPEXT_EIG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION+TFILM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Eigenfrequency"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DMPEXT_EIG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DNSOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DNSOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DNSOL2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DNSOL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DOF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add DOF"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DOF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DOF_DEL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Remove DOFs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DOF_DEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DOMEGA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DOMEGA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DOMEGA_dele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DOMEGA_dele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DSCALE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Constraints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DSCALE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DSYM_aN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((VECTORP+edgeflx)*NOFILTER))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DSYM_aN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DSYM_aP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((VECTORP+edgeflx)*NOFILTER))]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DSYM_aP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DSYM_asym) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DSYM_asym__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DSYM_symm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DSYM_symm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DSYS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Specified Coord Sys"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DSYS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DTRAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Constraints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DTRAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DUMP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Binary Files"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DUMP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DUMP1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hid"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DUMP1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DUMP2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hid"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DUMP2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DUMP_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DUMP_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DUMP_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DUMP_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_ALEADD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Set ALE Defaults"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_ALEADD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_ALEDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete ALE Defaults"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_ALEDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_ALEDEal) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete All"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_ALEDEal__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_ALELIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List ALE Defaults"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_ALELIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_ALELSal) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List All"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_ALELSal__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_ActCon) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Activate Entity"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_ActCon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_AdList) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_AdList__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Assem) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Assembly Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Assem__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Assem1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Assembly Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Assem1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Assem2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Assembly Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Assem2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_BND) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Symm Bndry Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_BND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_CPU) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "CPU Limit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_CPU__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_CSdele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_CSdele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_CSlist) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_CSlist__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_ConMore) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA+PREP7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Additional Parms"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_ConMore__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Contopt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA+PREP7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Advanced Controls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Contopt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Coord) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Create Local CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Coord__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_CurvAdd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add Curve"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_CurvAdd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_CurvDel) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Curve"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_CurvDel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_CurvLis) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Curve"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_CurvLis__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_CurvPlt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Curve"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_CurvPlt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Damp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA)+(SOLUTION*PREP7))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Damping"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Damp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_DeacCon) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Deactvate Entity"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_DeacCon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_DefBox) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Box"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_DefBox__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_DelCon) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Entity"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_DelCon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Double) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Double Precision"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Double__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_EDAC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global Settings"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_EDAC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_EDGC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_EDGC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_EDNB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Non-Refl Bndry"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_EDNB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_EDRC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Controls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_EDRC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_EDRD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Switch"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_EDRD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_EDRI) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Inertia Property"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_EDRI__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_EDTERls) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_EDTERls__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_EDTERnd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On a Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DYNA_EDTERnd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_EDTERpt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On a Part"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_EDTERpt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_EDTEnd2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On a Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_EDTEnd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Edadapt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Apply to Part"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Edadapt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Edhist) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Select Component"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Edhist__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Edint) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+SOLUTION)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Integ Pt Storage"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Edint__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Edopt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Output File Types"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Edopt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Edout) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+SOLUTION)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ASCII Output"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Edout__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Energy) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+SOLUTION)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Energy Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Energy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Hourgls) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Hourgls__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Input) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Write Jobname.k"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Input__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_LisCont) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Entities"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_LisCont__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Load) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+(SOLUTION*PREP7))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Specify Loads"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Load__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_MatProp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Local"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_MatProp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Par1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Par1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Par2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Par2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Par3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Par3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Par4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Par4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Par5) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Par5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Par6) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Par6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Par7) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Par7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Par8) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Contact Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Par8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Paralel) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Parallel Option"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Paralel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Params) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Contact"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Params__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Parts) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Parts Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Parts__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Parts_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Parts Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Parts_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Parts_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Parts Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Parts_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Parts_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Parts Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Parts_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_PltCont) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Select and Plot"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_PltCont__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Relax) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Dynamic Relax"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Relax__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Rigid) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+PREP7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Rigid Body CE"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Rigid__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_RstHt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+SOLUTION)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Number of Steps"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_RstHt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_RstHtDt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+SOLUTION)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Time Step Size"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_RstHtDt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Scale) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Time Step Ctrls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Scale__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Shell) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Shell Elem Ctrls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Shell__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Spotgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+PREP7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Genrlizd Spotwld"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Spotgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Spotwld) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+PREP7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Massless Spotwld"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DYNA_Spotwld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Start) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Restart Option"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Start__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_StrInit) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Initial Stress"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_StrInit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Subcycl) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Subcycling"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Subcycl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Term) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA+SOLUTION)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solution Time"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Term__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_TmHis2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA)+SOLUTION)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Time Hist Ctrls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_TmHis2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_TmHis3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA)+SOLUTION)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Time Hist Ctrls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_TmHis3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_VelDele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_VelDele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_VelExe1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Execute EDVE command"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_VelExe1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_VelExe2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Execute EDVE command"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_VelExe2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_VelGene) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "w/Axial Rotate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_VelGene__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_VelInpt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "w/Nodal Rotate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_VelInpt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_VelList) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_VelList__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Visco) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Bulk Viscosity"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Visco__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Wlddele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+PREP7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Wlddele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_Wldlist) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+PREP7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_Wldlist__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_adBND) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete All"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_adBND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_adEDNB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete All"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_adEDNB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_adLoad) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+(SOLUTION*PREP7))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete All"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_adLoad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_adapstat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_adapstat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_dBND) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Individ"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_dBND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_dEDNB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Individ"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_dEDNB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_dLoad) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+(SOLUTION*PREP7))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Individ"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_dLoad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNA_globstat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNA_globstat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNApVelDele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNApVelDele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNApVelGene) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "w/Axial Rotate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNApVelGene__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNApVelInpt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "w/Nodal Rotate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNApVelInpt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNApVelList) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNApVelList__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNROT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Rotated Nodal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNROT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYN_cnst) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Additional Nodal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYN_cnst__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYN_cnst1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Additional Nodal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYN_cnst1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DYNexport) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Read Disp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DYNexport__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeflx+VECTORP)*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_a2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_a2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_azP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(EDGEFLX*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_azP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_current) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Fix current displacement"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_current__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_enke) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_enke__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_enke2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_enke2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_mag) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeflx+SCALARP)*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_mag__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_mag2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_mag2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_magN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SCALARP*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Flux Normal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_magN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_magNN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeflx+SCALARP)*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_magNN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_magP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((edgeflx+SCALARP)*NOFILTER))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Flux Parallel"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_magP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_mult_del) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(MULTIPNT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_mult_del__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_mult_del2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_mult_del2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_multi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(MULTIPNT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_multi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_multi2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_multi2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_pec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (((PREP7*SOLUTION)+EDGEELC)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_pec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_psd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_psd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_psd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_psd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_psd_dele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_psd_dele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_psd_dele2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_psd_dele2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_spec) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_spec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_turb) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_turb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_u) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_u__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_u2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_u2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_u2CM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_D_u2CM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_uCFD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_uCFD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_uCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Node Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_uCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_v) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)+(CFD*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_v__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_volt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_volt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_volt0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_volt0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_volt0_ti) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(elem3d+edgeelc+TIMEINTP)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_volt0_ti__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_D_volt_ti) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(TIMEINTP*NOFILTER)+edgeelc)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_D_volt_ti__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Damping) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((STRUCTRL+static+buckling)*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Damping"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Damping__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DecOpt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Specifications"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DecOpt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Dec_Stat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Decimation Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Dec_Stat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Def_port) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define port"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Def_port__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DelBodyFor) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_CFD*pr_set)+(lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (CFD*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Body Forces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DelBodyFor__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DelInitCon) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STRUCTRL*THERMAL*FLUID*ELECTRIC*MAGNETIC*CFD))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Initial Condit'n"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DelInitCon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Diode) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Diode"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Diode__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Diode_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Diode_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DomainDcomp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PARPERF+PREP7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Domain Decomp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DomainDcomp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DropControl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Set Up"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DropControl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DropControl2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DropControl2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DropNode1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick 2 Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DropNode1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DropNode2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick 2 Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_DropNode2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DtAndyna) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (DROPPOST*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Animate Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DtAndyna__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DtSolve) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (DTFILT)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solve"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DtSolve__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DtTmHis) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (DROPPOST*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "%_zlabl% Variables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DtTmHis__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DtTmHis2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DtTmHis2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DtTmHis3gl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DtTmHis3gl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DtTmHis3sc) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DtTmHis3sc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DtTmHis3sc2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DtTmHis3sc2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DtTmHis4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (DROPPOST*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "%_zlabl% Variables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DtTmHis4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DtTmHisPlot) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Graph Variables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DtTmHisPlot__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_DtTmHisPrnt) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List Variables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_DtTmHisPrnt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_E) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Thru Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_E__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EALIVE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Activate Elem"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EALIVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDELE_pret) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pre-tens Elemnts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EDELE_pret__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDELE_pret2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pre-tens Elemnts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EDELE_pret2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDELE_pret3n) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pre-tens Elemnts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EDELE_pret3n__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDELE_pret3y) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Pre-tens Elemnts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EDELE_pret3y__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDEL_ME0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDEL_ME0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDEL_ME1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDEL_ME1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDEL_ME2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDEL_ME2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDEL_MEF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDEL_MEF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDEL_MEM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EDEL_MEM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDEL_MFF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDEL_MFF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDFPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Show Forces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDFPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDG_GAUGE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((ELEM3D+EDGEFLX)*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Gauging"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDG_GAUGE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDIPART_def) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Inertia"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDIPART_def__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDIPART_defB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Inertia"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDIPART_defB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDIPART_defC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Inertia"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDIPART_defC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDIPART_defD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Inertia"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDIPART_defD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDIPART_defE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Inertia"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDIPART_defE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDIPART_defF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Inertia"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDIPART_defF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDIPART_defX) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Inertia"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDIPART_defX__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDIPART_del) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Inertia"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDIPART_del__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDIPART_lis) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Inertia"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDIPART_lis__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDLDPLOT_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((LSDYNA*FULL_ANS)+(SOLUTION*PREP7))]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Load Curve"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDLDPLOT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDNDTSD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION*PREP7*POST26)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Smooth Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDNDTSD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDNDTSD_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDNDTSD_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDNDTSD_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION*PREP7*POST26)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDNDTSD_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDNDTSD_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION*PREP7*POST26)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDNDTSD_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDPRTLST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List Parts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDPRTLST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDPRTPLT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Parts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDPRTPLT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDPRTSEL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select Parts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDPRTSEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDTIMSTP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Time Step Prediction"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDTIMSTP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EDTP2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Time Steps"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EDTP2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EGEN_copy) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Auto Numbered"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EGEN_copy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EGEN_copy_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EGEN_copy_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EINTF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "At Coincid Nd"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EINTF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EINTF_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EINTF_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EINTF_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EINTF_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EINTF_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EINTF_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EINTF_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EINTF_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EINTF_OFFSET) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Offset Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EINTF_OFFSET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EKILL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Kill Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EKILL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ELBOW_all) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Selected set"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ELBOW_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ELBOW_node) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ELBOW_node__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ELBO_node_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Create Elbow Release"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ELBO_node_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ELIST_p_00) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes + Attributes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ELIST_p_00__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ELIST_p_01) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes + Attr + RealConst"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ELIST_p_01__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ELIST_p_10) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Attributes + RealConst"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ELIST_p_10__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ELIST_p_11) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Attributes Only"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ELIST_p_11__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ELMORPH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+RESULTS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ELMORPH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMAGERR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+(MAGNETIC*ELECTRIC))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((thermal+cfd+structrl+edgeflx+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Error Eval"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMAGERR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMA_HFEL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((HARMONIC+EDGEELC*MAGHFE)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Freq Sweep"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMA_HFEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMA_STAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((STATIC+SCALARP*VECTORP*EDGEFLX+harmonic)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Opt&Solv"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMA_STAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PR_ELMAG*pr_set)+(lsdyna))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((POST1+RESULTS+ELECTRIC*EDGEELC+vfed+circ+timeintp)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "EMF"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMFTMAC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+harmonic*ELEM2D)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELEM3D+EDGEFLX)*QUASISTA*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Summarize Force/Torque"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMFTMAC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMID_add) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add Mid Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMID_add__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMID_remove) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Remove Mid Nd"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMID_remove__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad42) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad42__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad50) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad50__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad62) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad62__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad7) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad72) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (HARMONIC)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad72__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad8) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad82) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad82__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad9) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad9__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMLoad92) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMLoad92__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMODIF_attr) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Attrib"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EMODIF_attr__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMODIF_attr2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMODIF_attr2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMODIF_node) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EMODIF_node__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMODIF_node2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMODIF_node2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMORP1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMORP1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMORP2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMORP2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMSYM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SCALARP*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Copy Sources"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMSYM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMTGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (edgeelc*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Node to Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMTGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMTGEN_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMTGEN_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMTGEN_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMTGEN_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMT_RC0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMT_RC0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMT_RC1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMT_RC1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMUNIT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Electromag Units"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMUNIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMUNIT2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMUNIT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMUNIT21) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMUNIT21__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EMUNIT3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EMUNIT3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFADELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BFADELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFA_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BFA_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFA_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EM_BFA_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BFDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFEDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BFEDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BFE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFE_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EM_BFE_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFKDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BFKDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFK_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BFK_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFK_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EM_BFK_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFLDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BFLDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFL_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BFL_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFL_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EM_BFL_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFVDELE_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BFVDELE_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFV_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Volumes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BFV_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BFV_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EM_BFV_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BF_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EM_BF_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_BF_temp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EM_BF_temp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_LDREAD_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From Therm Analy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EM_LDREAD_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EM_TUNIF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+ELECSTAT*EDGEELC*QUASISTA)*(MAGNETIC+thermal)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Uniform Temp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EM_TUNIF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Thru Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EN2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Thru Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EN2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ENDR_all) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Selected set"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ENDR_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ENDR_node) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ENDR_node__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ENDR_node_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Create End Release"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ENDR_node_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ENGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "User Numbered"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ENGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ENGEN_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ENGEN_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ENORM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Shell Normals"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ENORM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ENSYM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "User Numbered"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ENSYM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ENSYMM_rv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "of Shell Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ENSYMM_rv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ENSYM_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ENSYM_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EORIENT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+((pr_set*PR_MULTI*PR_STRUC)+(pmethod)+(lsdyna)))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS+ed)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Orient Normal"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EORIENT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EORIENT_dir) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EORIENT_dir__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EORIENT_pck) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Pick element"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EORIENT_pck__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "EPLOT"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EREAD_ERRANG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Read Elem File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EREAD_ERRANG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EREF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_EREF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EREF_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Refine Mesh at ELEM"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EREF_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EREF_2a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EREF_2a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EREF_2b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EREF_2b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EREF_ALL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "All"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EREF_ALL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EREF_ALL_2a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EREF_ALL_2a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EREF_ALL_2b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EREF_ALL_2b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ERESX) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Integration Pt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ERESX__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESCHECK_ESEL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((POST1*SOLUTION)+begin)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sel Warning/Error Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESCHECK_ESEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESCHECK_plt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((POST1*SOLUTION)+begin)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Warning/Error Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESCHECK_plt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESEL_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Also select elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESEL_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESEL_A_resul) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESEL_A_resul__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESEL_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reselect elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESEL_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESEL_R_resul) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESEL_R_resul__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESEL_S) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESEL_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESEL_S1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESEL_S1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESEL_S_resul) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESEL_S_resul__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESEL_U) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unselect elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESEL_U__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESEL_U_resul) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESEL_U_resul__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESEL_results) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESEL_results__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESIZE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Size"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESIZE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESIZE_ndiv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESIZE_ndiv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOL1A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOL1A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOL1e) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOL1e__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOL_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOL_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOL_Ae) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOL_Ae__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOL_SN) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elem res by seq no."]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOL_SN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOL_SNA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elem res by seq no."]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOL_SNA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOL_SNAe) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOL_SNAe__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOL_SNB) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOL_SNB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOL_SNe) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Elem res by seq no."]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOL_SNe__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOLe) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element Results"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOLe__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOLp1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elemental Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOLp1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESOLp2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESOLp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESORT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sort Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESORT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESSOLV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (edgeelc*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elec/struc"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESSOLV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESSOLV_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (edgeelc*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESSOLV_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESSOLV_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESSOLV_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESSOLV_7) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESSOLV_7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESTIF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "StiffnessMult"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESTIF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Surf to Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESURF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURF2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Free Surfaces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESURF2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURFA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (pr_struc*PR_THERM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area to Fluid"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESURFA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURFL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (pr_struc*PR_THERM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line to Fluid"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESURFL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURFLA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESURFLA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURFL_T) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (pr_struc*PR_THERM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESURFL_T__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURFN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (pr_struc*PR_THERM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Node to Fluid"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESURFN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURFN1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (pr_struc*PR_THERM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Extra Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESURFN1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURFN2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "No extra Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESURFN2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURFNO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESURFNO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURFN_T) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (pr_struc*PR_THERM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESURFN_T__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURFT_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Surf Effect"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESURFT_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURFT_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESURFT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURF_175) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Surf to Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESURF_175__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURF_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Free Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESURF_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURF_22) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Free Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESURF_22__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURF_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Free Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESURF_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURF_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Free Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESURF_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURF_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Surf to Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESURF_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESURF_575) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Surf to Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESURF_575__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESYM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Auto Numbered"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ESYM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ESYM_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ESYM_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ET) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add/Edit/Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ETABLE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Define Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ETABLE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ETABLE_ADD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ETABLE_ADD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ETABLE_erase) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Erase Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ETABLE_erase__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ETCHG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Switch Elem Type"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ETCHG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ETCONTROL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elem Tech Control"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ETCONTROL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ET_add) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ET_add__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EUSORT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Unsort Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EUSORT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EWRITE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Write Elem File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EWRITE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EXP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Exponentiate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EXP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EXPAND) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((POST1+RESULTS)+CYCGEN)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*PROFS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Cyc Expansion"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EXPAND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EXPASS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*(SOLUTION+lstepgt1))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((spectrum+cfd+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ExpansionPass"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EXPASS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EXPASS_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EXPASS_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EXPASS_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "ExpansionPass"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EXPASS_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EXPSOL_HREX2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((TRNSIENT+full)*(HARMONIC+full)*SUBSTRUC*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Time/Freq"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EXPSOL_HREX2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EXPSOL_HREXP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((TRNSIENT+full)*(HARMONIC+full)*SUBSTRUC*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Load Step"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EXPSOL_HREXP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EXTOPT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elem Ext Opts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EXTOPT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EXTOPT2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EXTOPT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EXTOPT_vswe) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sweep Opts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EXTOPT_vswe__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EXTREM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Extremes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EXTREM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Emiss_aux12) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (AUX12)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (pr_cfd+cfd)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Emissivities"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Emiss_aux12__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EuidlMat) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Material Models"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EuidlMat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_EuidlSol) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((STATIC*(TRNSIENT+FULL))+lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sol'n Controls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_EuidlSol__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ExitRout) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ExitRout__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Export) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Export"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Export__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Export_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ed)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Export IGES File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Export_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ExtPort) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Exterior"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ExtPort__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Forces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritChek) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Criteria Check"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritChek__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritDef) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Add/Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritDef__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritDef2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritDef2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritDef2a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Add/Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritDef2a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritDef2a1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Add/Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritDef2a1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritDef2b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Add/Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritDef2b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritDef2bx) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Add/Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritDef2bx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritDef2by) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Add/Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritDef2by__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritDele) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritDele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritList) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritList__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritTemp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Temp Variation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritTemp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FCritTemp2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Temp Variation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FCritTemp2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_amps) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+elecstat+vfed+circ+edgeelc+timeintp)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_amps__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_csg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_csg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_csg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FDELE_csg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_flow_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_flow_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_flux) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+SCALARP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_flux__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_fm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_fm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_fm2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FDELE_fm2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_fm2CM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FDELE_fm2CM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_fmCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Node Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_fmCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_heat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_heat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_heat2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FDELE_heat2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_ic) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+elecstat+circ+edgeelc+TIMEINTP)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_ic__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_multi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(MULTIPNT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_multi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_multi2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FDELE_multi2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FDELE_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_psd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_psd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_psd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FDELE_psd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_sv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SINGLEPT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FDELE_sv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FDELE_sv2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FDELE_sv2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Assign Events"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FELIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Event Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FELIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FE_erase) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Erase Event Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FE_erase__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FIDAP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "FIDAP"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FIDAP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FILL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Fill between Nds"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FILL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FILL2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create Nodes Between 2 Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FILL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FILL_p26) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Fill Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FILL_p26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FINISH) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Finish"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FINISH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FJDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(ELMJOINT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Joint Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FJDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FJDE_joint) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(ELMJOINT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Joint Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FJDE_joint__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FJDE_joint2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FJDE_joint2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FJLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Element"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FJLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FJ_joint) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(ELMJOINT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Joint Elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FJ_joint__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FJ_joint2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FJ_joint2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On All KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FKDELE_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_amps) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+elecstat+vfed+circ+edgeelc+timeintp)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDELE_amps__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDELE_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_csg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDELE_csg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_csg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FKDELE_csg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_flow_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDELE_flow_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_flux) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+SCALARP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDELE_flux__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_fm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDELE_fm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_fm2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FKDELE_fm2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_heat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDELE_heat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_ic) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+elecstat+circ+edgeelc+TIMEINTP)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDELE_ic__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_mult) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(MULTIPNT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDELE_mult__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_mult2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FKDELE_mult2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_psd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDELE_psd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_psd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FKDELE_psd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_sv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SINGLEPT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDELE_sv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDELE_sv2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FKDELE_sv2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDEL_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKDEL_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKDEL_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FKDEL_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FKLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FKLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_amps) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+elecstat+vfed+circ+edgeelc+timeintp)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_amps__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_amps_ic) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((ELECTRIC+elecstat+circ+edgeelc+TIMEINTP)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_amps_ic__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_csg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_csg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_csg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FK_csg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_flow_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_flow_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_flux) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+SCALARP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_flux__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_flux2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FK_flux2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_fm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_fm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_fm2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FK_fm2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_heat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_heat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_heat2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FK_heat2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_multi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(MULTIPNT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_multi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_multi2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FK_multi2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FK_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_psd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_psd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_psd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FK_psd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_sv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SINGLEPT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FK_sv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FK_sv2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FK_sv2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Stress Locations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Picked Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_FLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FLLIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Stress Loc"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FLLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FLUXV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+VECTORP)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((edgeflx+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Path Flux"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FLUXV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FMAGBC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+MAGNETIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((edgeelc+harmonic*ELEM2D)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Comp. Force/Torque"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FMAGBC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FMAGBCE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION+ELECSTAT*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Comp. Force"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FMAGBCE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FMAGBC_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FMAGBC_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FMAGBC_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FMAGBC_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FMAGBC_4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FMAGBC_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FMAGSUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+FMAGBC+harmonic*ELEM2D)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Force"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FMAGSUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FMAGSUM_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FMAGSUM_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FMAGSUM_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FMAGSUM_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FMAGSUM_4) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FMAGSUM_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FOR2D) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+ELEM2D+VECTORP+harmonic)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((edgeflx+edgeelc)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mag Forces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FOR2D__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "S-N Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FP2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FP2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FPLIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FPLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FP_erase) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Erase Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FP_erase__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FP_erase1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FP_erase1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FP_erase2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FP_erase2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FP_mn) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elas-plas Par"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FP_mn__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FP_smt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sm_T Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FP_smt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FP_smt2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FP_smt2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FREQ) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SINGLEPT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Freq Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FREQ__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FREQ1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FREQ1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FREQ2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FREQ2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FREQ_erase) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SINGLEPT*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Erase Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FREQ_erase__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Specified Val"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FS1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FS1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FS2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FS2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FS3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FS3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSCALE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Forces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSCALE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Dele Stresses"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSILoad) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSILoad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSI_Conv) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSI_Conv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSI_Iter) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Iterations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSI_Iter__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSI_Opt) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSI_Opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSI_Options) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSI_Options__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSI_Relax) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Relaxation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSI_Relax__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSI_Restart) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Restart"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSI_Restart__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSI_Stat) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSI_Stat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSI_Time) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Time"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSI_Time__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSI_tables) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSI_tables__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSI_values) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSI_values__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSLIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Stresses"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSNODE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From rst File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSNODE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Stresses"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSRS_tables) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Relaxation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSRS_tables__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSRS_values) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Relaxation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSRS_values__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSSECT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "At Cross Sect"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSSECT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSSPARM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((POST1+RESULTS+EDGEELC+HARMONIC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "FSS Parameters"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSSPARM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FSUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Total Force Sum"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FSUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FTCALC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Calculate Fatig"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FTCALC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FTCALC2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Calculate Fatig"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FTCALC2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FTRAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Forces"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FTRAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FTSIZE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Size Settings"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FTSIZE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FTWRIT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Write Fatig Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FTWRIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FUNC_EDIT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Define/Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FUNC_EDIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FUNC_READ) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Read File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FUNC_READ__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FVMESH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FVMESH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_amps) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELECTRIC+elecstat+vfed+circ+edgeelc+timeintp)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_amps__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_amps_ic) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((ELECTRIC+elecstat+circ+edgeelc+TIMEINTP)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_amps_ic__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_chrg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set*PR_MULTI*(PR_THERM+ELECTRIC))]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list ((ELECSTAT+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_chrg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_csg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+VECTORP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_csg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_csg2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_F_csg2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_flow_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(cfd*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_flow_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_flux) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((MAGNETIC+SCALARP+edgeflx)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_flux__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_flux2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_F_flux2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_fm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_fm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_fm2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_F_fm2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_fm2CM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_F_fm2CM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_fmCM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Node Components"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_fmCM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_heat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_heat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_heat2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_F_heat2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_multi) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(MULTIPNT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_multi__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_multi2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_F_multi2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_F_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_psd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_psd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_psd2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_F_psd2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_sv) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SINGLEPT*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_F_sv__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_F_sv2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_F_sv2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FileOpt2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FileOpt2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FileOpts) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "File Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FileOpts__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FloLoad) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FloLoad__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FloLoad10) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FloLoad10__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FloLoad20) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FloLoad20__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FloLoad50) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FloLoad50__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FloTurb) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FloTurb__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FloVelo) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FloVelo__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FlowTrace) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+CFD*ELECTRIC*MAGNETIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Flow Trace"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FlowTrace__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Forc_C1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Forc_C1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Forc_C2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Forc_C2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Forc_C3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Forc_C3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Forc_C4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Forc_C4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Forc_C5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Forc_C5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Forc_C6) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Forc_C6__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Forc_C7) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Forc_C7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Forc_C8) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Forc_C8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Forc_Q) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Forc_Q__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Forc_T) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Forc_T__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FullGui) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((STATIC*TRNSIENT*BUCKLING*MODAL)+simploff)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Unabridged Menu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FullGui__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_FullMatGui) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (cfd+pr_cfd+unabrmat)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Unabridged Menu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_FullMatGui__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GAPF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Gap Force"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_GAPF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GLREAD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "GLSTAT file"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_GLREAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GMATRIX) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (QUASISTA*(SCALARP+ELEM3D)*(REGLFMAG+ELECTRIC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Self/Mutual conductance"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_GMATRIX__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_GP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GP2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_GP2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GPDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_GPDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GPLIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Specified"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_GPLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GPLIST_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List All"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_GPLIST_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GSBloads) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Gen Plane Strain"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_GSBloads__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GSG) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Genl plane strn"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_GSG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GSUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Of Geometry"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_GSUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Geom) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Update Geom"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Geom__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GlobalStat) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Global Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_GlobalStat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_GrphSet1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_GrphSet1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HARFRQ_etc) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(HARMONIC*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Freq and Substeps"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HARFRQ_etc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HBMAT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Dump Matrix"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HBMAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HEXMAP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HEXMAP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HFFAR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Far Field"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HFFAR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HFFAR_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Far Field"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HFFAR_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HFFAR_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HFFAR_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HFFAR_L) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Far Field"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HFFAR_L__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HFNEAR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (ELECTRIC*MAGNETIC+EDGEELC)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Near Field"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HFNEAR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HFNEAR_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HFNEAR_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HFSWEEP0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HFSWEEP0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HFSWEEP1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HFSWEEP1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HFSWEEP2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HFSWEEP2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HF_PLSP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((EDGEELC+ELEM3D)*NOFILTER+lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "S-Parameters"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HF_PLSP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HMAGSOLV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((HARMONIC+ELEM2D)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Opt&Solv"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HMAGSOLV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HPTAC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hard PT by coordinates"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_HPTAC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HPTAC_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create Hard PT by Coordinates"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HPTAC_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HPTAP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hard PT by picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_HPTAP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HPTAP_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hard PT by picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HPTAP_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HPTDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hard Points"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_HPTDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HPTLC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hard PT by coordinates"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_HPTLC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HPTLC_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create Hard PT by Coordinates"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HPTLC_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HPTLP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hard PT by picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_HPTLP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HPTLP_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hard PT by picking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HPTLP_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HPTLR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hard PT by ratio"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_HPTLR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HPTLR_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create Hard PT by Ratio"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HPTLR_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HROCEAN_etc) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(HARMONIC*NOFILTER)+PIPEEXIS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "HOWP and Substeps"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HROCEAN_etc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HomePage) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Menu Name"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HomePage__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HomePage_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Menu Name"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HomePage_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_HostSelect) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna+OLDMENU+PPDSPAR)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (HSCRIPT)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Host Selection"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_HostSelect__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ICLIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Picked"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_ICLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ICLIST1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ICLIST1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ICLIST2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ICLIST2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ICLIST_all) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List All"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ICLIST_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IC_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IC_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IC_p2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_IC_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IDEAS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Import IDEAS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_IDEAS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMAGIN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST26+HARMONIC)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Imaginary Part"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_IMAGIN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH2D) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)+(pr_set*PR_MULTI*PR_STRUC)+(pmethod)+(lsdyna)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "2D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH2D__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH2Da) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)+(pr_set*PR_MULTI*PR_STRUC)+(pmethod)+(lsdyna)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "2D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH2Da__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH2Db) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "2D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH2Db__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH2Dc) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "2D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH2Dc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH2Dd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "2D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH2Dd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH2Df) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "2D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH2Df__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH3D) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)+(pr_set*PR_MULTI*PR_STRUC)+(pmethod)+(lsdyna)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "3D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH3D__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH3Da) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)+(pr_set*PR_MULTI*PR_STRUC)+(pmethod)+(lsdyna)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "3D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH3Da__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH3Db) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "3D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH3Db__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH3Dc) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "3D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH3Dc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH3Dd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "3D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH3Dd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMESH3Df) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "3D Interface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_IMESH3Df__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMPD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((POST1+RESULTS+EDGEELC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Impedance"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_IMPD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMPD_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_IMPD_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IMPD_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_IMPD_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_INDMATR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((STATIC+SCALARP*VECTORP*EDGEFLX*MAGEDG+harmonic)*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Induct Matrix"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_INDMATR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_INDMATR_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_INDMATR_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_INDMATR_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_INDMATR_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_INDMATR_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_INDMATR_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_INDMATR_21) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_INDMATR_21__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_INDMATR_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_INDMATR_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_INDMATR_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_INDMATR_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_INT1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Integrate"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_INT1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_INTSRF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Surface Integral"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_INTSRF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IRLF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STATIC*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Inertia Relief"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_IRLF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IRLF_-1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STATIC*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Incl Mass Summry"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_IRLF_-1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IRLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Inertia Relief Summary"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_IRLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ISFILE_del) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Stresses"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ISFILE_del__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ISFILE_list) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Stresses"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ISFILE_list__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ISFILE_read) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Read IS File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ISFILE_read__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ISFILE_read1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Read IS File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ISFILE_read1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ISFILE_read2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Read IS File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ISFILE_read2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ISTRESS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Apply Const Strs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ISTRESS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ISTRESS_ex1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Apply Const Strs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ISTRESS_ex1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ISTRESS_ex2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Apply Const Strs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ISTRESS_ex2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_ISWRITE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Write Stresses"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_ISWRITE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Iconscale) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Scale Icon"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Iconscale__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Import) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Import"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Import__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Import_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Import IGES File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Import_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Import_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Import IGES File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Import_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Import_x) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Import_x__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_InfAElm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Inf Acoustic"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_InfAElm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_IntPort) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (EDGEELC*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Interior"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_IntPort__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_J-Parall) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (((ELECTRIC*ELECSTAT)+timeintp+edgeelc)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "J-Parallel"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_J-Parall__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_J-Parall-T) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(elem3d+edgeelc+TIMEINTP)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "J-Parallel"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_J-Parall-T__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_JointAddEdit) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add / Edit"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_JointAddEdit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_K) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "In Active CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_K__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KATT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KATT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KATT_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KATT_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KATT_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KATT_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KBET) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "KP between KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KBET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KBET_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "KBETween options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KBET_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KCALC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Stress Int Factr"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KCALC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KCEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "3 keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KCEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KCENK_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Center of 3 KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KCENK_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KCENL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Location on line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KCENL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KCENL_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Center of line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KCENL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KCENL_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Center of line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KCENL_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KCENR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "3 KPs and radius"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KCENR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KCENR_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "KCENter command options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KCENR_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KCLEAR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KCLEAR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KDIS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "KP distances"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KDIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KESIZE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KESIZE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KESIZE1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KESIZE1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KESIZE2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KESIZE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KESIZE_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KESIZE_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KESIZE_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KESIZE_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KESIZE_p3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KESIZE_p3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KESIZE_p4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KESIZE_p4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KESIZE_pdel) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clr Size"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KESIZE_pdel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KESIZE_pl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KESIZE_pl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KESIZEl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KESIZEl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEY116_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 116"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEY116_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEY116_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 116"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEY116_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEY116_12) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 116"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEY116_12__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEY116_16) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 116"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEY116_16__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEY116_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 116"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEY116_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_0) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 0"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 1"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_10) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 10"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_10__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_106) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 106"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_106__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_107) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 107"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_107__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_108) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 108"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_108__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_109) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 109"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_109__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_11) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 11"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_11__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_110) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 110"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_110__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_111) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 111"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_111__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_115) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 115"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_115__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_116) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 116"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_116__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_117) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 117"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_117__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_118) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 118"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_118__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_119) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 119"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_119__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_12) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 12"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_12__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_120) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 120"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_120__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_121) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 121"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_121__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_122) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 122"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_122__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_123) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 123"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_123__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_124) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 124"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_124__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_124a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 124"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_124a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_125) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 125"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_125__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_126) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 126"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_126__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_126future) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 126"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_126future__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_127) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 127"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_127__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_128) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 128"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_128__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_129) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 129"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_129__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_13) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 13"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_13__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_130) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 130"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_130__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_131) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 131"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_131__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_132) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 132"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_132__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_136) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 136"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_136__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_138) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 138"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_138__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_139) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 139"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_139__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_14) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 14"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_14__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_141) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 141"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_141__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_142) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 142"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_142__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_143) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 143"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_143__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_144) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 144"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_144__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_145) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 145"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_145__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_146) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 146"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_146__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_147) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 147"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_147__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_148) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 148"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_148__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_15) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 15"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_15__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_150) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 150"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_150__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_151) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 151"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_151__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_152) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 152"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_152__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_153) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 153"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_153__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_154) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 154"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_154__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_156) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 156"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_156__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_157) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 157"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_157__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_158) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 158"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_158__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_159) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 159"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_159__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_16) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 16"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_16__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_160) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 160"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_160__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_161) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 161"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_161__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_162) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element 162"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_162__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_162A) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element 162"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_162A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_163) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 163"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_163__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_164) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_164__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_165) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 165"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_165__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_166) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 166"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_166__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_167) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 167"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_167__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_168) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 168"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_168__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_169) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 169"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_169__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_169A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 169"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_169A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_17) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 17"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_17__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_170) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 170"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_170__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_170A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 170"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_170A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_171) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 171"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_171__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_172) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 172"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_172__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_173) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 173"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_173__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_174) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 174"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_174__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_175) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 175"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_175__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_176) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 176"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_176__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_177) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 177"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_177__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_178) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 178"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_178__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_179) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 179"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_179__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_18) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 18"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_18__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_180) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 180"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_180__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_181) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 181"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_181__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_182) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 182"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_182__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_183) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 183"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_183__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_184) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 184"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_184__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_184a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 184"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_184a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_184b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 184"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_184b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_184c) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 184"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_184c__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_184d) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 184"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_184d__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_184e) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 184"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_184e__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_185) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 185"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_185__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_186) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 186"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_186__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_187) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 187"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_187__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_188) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 188"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_188__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_189) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 189"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_189__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_19) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 19"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_19__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_190) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 190"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_190__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_191) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 191"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_191__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_192) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 192"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_192__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_193) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 193"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_193__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_194) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 194"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_194__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_195) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 195"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_195__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 2"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_20) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 20"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_20__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_200) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 200"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_200__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_201) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 201"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_201__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_202) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 202"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_202__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_203) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 203"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_203__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_204) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 204"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_204__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_205) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 205"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_205__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_208) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 208"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_208__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_209) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 209"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_209__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_21) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 21"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_21__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_212) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 212"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_212__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_213) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 213"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_213__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_214) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 214"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_214__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_215) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 215"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_215__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_216) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 216"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_216__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_217) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 217"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_217__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_22) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 22"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_22__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_220) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 220"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_220__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_221) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 221"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_221__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_223) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 223"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_223__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_226) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 226"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_226__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_227) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 227"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_227__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_23) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 23"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_23__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_230) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 230"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_230__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_231) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 231"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_231__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_232) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 232"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_232__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_233) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 233"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_233__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_236) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 236"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_236__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_237) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 237"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_237__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_238) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 238"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_238__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_239) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 239"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_239__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_24) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 24"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_24__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_240) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 240"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_240__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_241) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 241"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_241__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_242) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 242"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_242__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_25) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 25"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_25__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_251) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 251"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_251__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_252) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 252"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_252__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_26) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 26"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_263) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 263"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_263__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_264) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 264"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_264__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_265) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 265"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_265__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_27) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 27"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_27__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_272) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 272"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_272__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_273) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 273"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_273__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_278) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 278"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_278__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_279) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 279"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_279__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_28) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 28"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_28__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_281) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 281"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_281__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_285) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 285"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_285__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_288) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 288"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_288__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_289) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 289"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_289__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_29) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 29"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_29__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_290) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 290"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_290__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 3"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_30) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 30"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_30__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_31) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 31"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_31__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_32) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 32"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_32__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_33) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 33"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_33__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_34) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 34"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_34__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_35) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 35"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_35__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_36) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 36"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_36__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_37) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 37"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_37__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_38) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 38"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_38__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_39) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 39"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_39__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 4"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_40) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 40"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_40__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_41) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 41"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_41__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_42) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 42"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_42__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_43) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 43"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_43__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_44) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 44"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_44__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_45) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 45"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_45__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_46) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 46"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_46__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_47) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 47"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_47__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_48) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 48"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_48__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_49) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 49"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_49__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_5) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 5"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_5__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_50) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 50"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_50__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_51) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 51"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_51__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_52) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 52"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_52__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_53) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 53"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_53__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_54) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 54"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_54__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_55) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 55"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_55__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_56) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 56"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_56__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_57) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 57"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_57__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_58) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 56"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_58__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_59) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 59"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_59__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_60) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 60"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_60__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_61) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 61"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_61__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_62) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 62"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_62__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_63) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 63"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_63__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_64) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 64"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_64__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_65) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 65"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_65__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_66) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 66"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_66__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_67) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 67"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_67__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_68) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 68"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_68__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_69) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 69"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_69__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_7) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 7"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_7__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_70) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 70"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_70__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_71) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 71"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_71__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_74) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 74"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_74__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_75) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 75"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_75__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_77) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 77"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_77__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_78) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 78"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_78__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_79) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 79"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_79__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_8) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 8"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_8__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_80) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 80"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_80__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_81) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 81"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_81__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_82) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 82"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_82__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_83) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 83"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_83__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_84) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 84"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_84__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_86) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 86"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_86__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_87) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 87"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_87__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_88) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 88"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_88__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_89) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 89"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_89__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_9) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 9"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_9__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_90) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 90"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_90__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_91) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 91"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_91__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_92) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 92"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_92__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_93) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 93"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_93__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_94) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 94"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_94__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_94a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 94"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_94a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_95) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 95"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_95__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_96) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 96"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_96__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_97) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 97"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_97__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_97a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 97"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_97a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_98) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 98"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_98__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KEYOPT_99) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Element 99"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KEYOPT_99__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KFILL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Fill between KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KFILL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KFILL2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Fill between KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KFILL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KGEN2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KGEN2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KGEN_move) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Set of KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KGEN_move__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KGEN_move_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Set of Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KGEN_move_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KLD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Line w/Ratio"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KLD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KLIST_HardPt) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hard Points"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KLIST_HardPt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KLIST_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Coords +Attributes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KLIST_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KLIST_p_coor) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Coordinates Only"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KLIST_p_coor__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KL_d2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Line w/Ratio"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KL_d2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KMESH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KMESH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KMESH_m) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+meshtool)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KMESH_m__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KMODIF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Single KP"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KMODIF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KMOVE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "To Intersect"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KMOVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KMOVE_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KMOVE_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KNODE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KNODE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KPSCAL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KPSCAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KPSCAL_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KPSCAL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KREF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KREF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KREF_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Refine Mesh at KP"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KREF_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KREF_2a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KREF_2a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KREF_2b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KREF_2b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSCON) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KSCON__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSCON2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Concentration KP"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KSCON2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSCON_stat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KSCON_stat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSELH_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Also select hard kpts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KSELH_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSELH_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reselect hard kpts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KSELH_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSELH_S) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select hard kpts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KSELH_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSELH_U) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unselect hard kpts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KSELH_U__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSEL_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Also select keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KSEL_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSEL_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reselect keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KSEL_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSEL_S) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KSEL_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSEL_U) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unselect keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KSEL_U__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Of Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KSUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSYMM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KSYMM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KSYMM_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KSYMM_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KTRAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KTRAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KTRAN_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KTRAN_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KUSE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (STATIC*(HARMONIC+FULL)*(TRNSIENT+FULL)*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reuse LN22 Matrix"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_KUSE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KWPA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KWPA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_KWPL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Keypoints"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_KWPL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_K_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Working Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_K_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_L) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "In Active Coord"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_L__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_L2ANG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Angle to 2 Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_L2ANG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_L2ANG_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Angle to 2 Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_L2ANG_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_L2ANG_90) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Norm to 2 Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_L2ANG_90__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_L2TAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Tan to 2 Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_L2TAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_L2TAN_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Tan to 2 Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_L2TAN_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LANG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "At angle to line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LANG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LANG_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Tangent to Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LANG_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LANG_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "At angle to line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LANG_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LANG_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "At angle to line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LANG_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LANG_90) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Normal to Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LANG_90__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LARC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By End KPs & Rad"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LARC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LARC2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LARC2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LARC_3kp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Through 3 KPs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LARC_3kp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LAREA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Overlaid on Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LAREA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LARGE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Find Maximum"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LARGE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LATT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LATT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LATT_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LATT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LATT_KP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LATT_KP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LATT_NOKP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LATT_NOKP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LATT_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LATT_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LATT_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LATT_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LATTp_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LATTp_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LATTp_KP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LATTp_KP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LATTp_NOKP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LATTp_NOKP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LAYER_p2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LAYER_p2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LAYER_p3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LAYER_p3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LAYLIS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Layered Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LAYLIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LAYPLO) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Layered Elements"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LAYPLO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LAYPLO2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LAYPLO2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCABS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Absolut Value"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCABS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCASE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Read Load Case"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCASE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCCAT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LCCAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCCAT_del) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCCAT_del__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCCAT_m) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LCCAT_m__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCDEF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCDEF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCDEF_eras1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCDEF_eras1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCDEF_eras2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCDEF_eras2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCDEF_erase) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Erase Load Case"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCDEF_erase__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCDEF_stat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Load Cases"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCDEF_stat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCFACT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Scale Factor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCFACT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCFILE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCFILE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCLEAR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LCLEAR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCOMB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LCOMB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCOMB2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCOMB2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCOPER_add) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCOPER_add__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCOPER_lprin) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line Elem Stress"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCOPER_lprin__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCOPER_mn_mx) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Min & Max"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCOPER_mn_mx__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCOPER_sqrt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Square Root"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCOPER_sqrt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCOPER_squa) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Square"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCOPER_squa__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCOPER_srss) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "SRSS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCOPER_srss__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCOPER_sub) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Subtract"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCOPER_sub__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCSEL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sele Ld Cases"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCSEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCSL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines by Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LCSL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCWRITE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Write Load Case"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCWRITE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LCZERO) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Zero Load Case"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LCZERO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LC_create) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create Load Case"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LC_create__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines Only"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDELE_sweep) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line and Below"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LDELE_sweep__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDIV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line into N Ln's"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LDIV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDIV_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line into 2 Ln's"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LDIV_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDIV_B) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line into N Ln's"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDIV_B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDIV_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines w/ Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LDIV_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDIV_opt_B) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDIV_opt_B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Transient only"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDPLOT2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Dynamic relax"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDPLOT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDPLOT3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Trans and Dynam"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDPLOT3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDRAG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Along Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LDRAG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_EF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELEM3D+MAGNETIC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From Elec An"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_EF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_dyna) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Temp From ANSYS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_dyna__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_film) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From Fluid Analy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_film__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_hfhf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From EMAG Analy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_hfhf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_hflu) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From Fluid Analy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_hflu__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_hgen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From Mag Analy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_hgen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_js) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_ELMAG*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((ELEM3D+MAGNETIC+VECTORP*EDGEFLX)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From Elec An"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_js__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_mforc) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From Mag Analy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_mforc__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_pres) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From Fluid Analy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_pres__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_reac) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From Reactions"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_reac__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_temp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From Therm Analy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_tempD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION+CFD*FLUID)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From ANSYS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_tempD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LDREAD_tempIC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION+(CFD*FLUID*THERMAL))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Temp from ANSYS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LDREAD_tempIC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LECOPY) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Copy Divs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LECOPY__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LECOPY2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LECOPY2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LEFLIP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Flip Bias"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LEFLIP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LELAYER_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LELAYER_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LELAYER_pdel) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clr Layers"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LELAYER_pdel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LESIZE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LESIZE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LESIZE1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LESIZE1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LESIZE2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LESIZE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LESIZE_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LESIZE_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LESIZE_p1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Picked Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LESIZE_p1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LESIZE_pdel) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clr Size"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LESIZE_pdel__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LESIZEl) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LESIZEl__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LESI_pclr) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LESI_pclr__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LESI_pclr1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LESI_pclr1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LEXTND) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Extend Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LEXTND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LEXTND_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Extend Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LEXTND_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LFILLT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line Fillet"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LFILLT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LFILLT_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line Fillet"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LFILLT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LGEN2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LGEN2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LGEN_move) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LGEN_move__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LGEN_move_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LGEN_move_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LGLUE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LGLUE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LINA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line with Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LINA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LINE_PK) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LINE_PK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LINE_PK_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LINE_PK_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LINL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LINL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LINP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LINP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LINV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line with Volume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LINV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LLIST_OPT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LLIST_OPT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LLIST_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LLIST_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LMESH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LMESH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LMESH_m) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+meshtool)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LMESH_m__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LNCOLLAPSE) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Collapse Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LNCOLLAPSE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LNSRCH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line Search"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LNSRCH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LOADPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Load Curve"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LOADPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LOCAL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "At Specified Loc"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LOCAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LOCAL2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LOCAL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LOVLAP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LOVLAP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LPATH) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Path"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LPATH__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LPTN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LPTN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LREF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LREF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LREF_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Refine Mesh at LINE"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LREF_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LREF_2a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LREF_2a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LREF_2b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LREF_2b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LREVERSE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "of Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LREVERSE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LREVERSE2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LREVERSE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LROTAT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "About Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LROTAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LROTAT_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "About Axis"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LROTAT_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBA_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBA_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBA_div_op2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSBA_div_op2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBA_div_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBA_div_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBL_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBL_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBL_div_op2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSBL_div_op2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBL_div_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBL_div_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBL_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBL_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBL_opt_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSBL_opt_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBV_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by Volume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBV_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBV_div_igs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by Volu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBV_div_igs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBV_div_op2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by Volume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSBV_div_op2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBV_div_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by Volume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBV_div_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBW_div) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBW_div__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBW_div_igs) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBW_div_igs__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBW_div_op2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSBW_div_op2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSBW_div_opt) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line by WrkPlane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSBW_div_opt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSCLEAR_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Loads & Opts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSCLEAR_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSCLEAR_fe) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All F.E. Loads"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSCLEAR_fe__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSCLEAR_iner) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All Inertia Lds"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSCLEAR_iner__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSCLEAR_lfac) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reset Factors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSCLEAR_lfac__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSCLEAR_lsop) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION+pr_cfd+cfd)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reset Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSCLEAR_lsop__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSCLEAR_soli) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "All SolidMod Lds"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSCLEAR_soli__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete LS Files"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSELH_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Also select lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSELH_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSELH_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reselect lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSELH_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSELH_S) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSELH_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSELH_U) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unselect lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSELH_U__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSEL_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Also select lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSEL_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSEL_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reselect lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSEL_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSEL_S) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSEL_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSEL_U) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unselect lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSEL_U__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSREAD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Read LS File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSREAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSSCAL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSSCAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSSCAL_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSSCAL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSSOLVE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (pp*LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "From LS Files"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSSOLVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSTR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Straight Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSTR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSUM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Of Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSWRITE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Write LS File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSWRITE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSYMM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LSYMM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LSYMM_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LSYMM_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LTAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Tangent to Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LTAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LTAN_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LTAN_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LTRAN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (priges)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LTRAN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LTRAN_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LTRAN_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LVSCALE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (MODESUP*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "For Mode Super"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LVSCALE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LVSCALE_PSD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PSDRANDM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "For PSD"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LVSCALE_PSD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LVSCALE_del_psd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PSDRANDM*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "For PSD"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LVSCALE_del_psd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LVSCALE_dele) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (MODESUP*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "For Mode Super"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_LVSCALE_dele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LWPL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plane Normal to Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LWPL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LWPL_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plane Normal to Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LWPL_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LWPL_3a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plane Normal to Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LWPL_3a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_LWPL_3b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plane Normal to Line"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_LWPL_3b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_M) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_M__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_M2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_M2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MAGOPT_dsp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SCALARP*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "DSP Method"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MAGOPT_dsp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MAGOPT_gsp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SCALARP*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "GSP Method"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MAGOPT_gsp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MAGOPT_rsp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(SCALARP*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "RSP Method"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MAGOPT_rsp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MAGSOLV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (harmonic+trnsient)*(NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (edgeelc*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Opt and Solv"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MAGSOLV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MAGSOLV_MSP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MAGSOLV_MSP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MAGSOLV_MVP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MAGSOLV_MVP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MATREAD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (LSDYNA*FULL_ANS)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "MATSUM file"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MATREAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MAT_TYPE_gen) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Elem Attributes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MAT_TYPE_gen__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MCHECK) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+begin)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Ck Connectvty"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MCHECK__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MCHECK_ESEL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+begin)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sel Bad Connt"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MCHECK_ESEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_MDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MDELE2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_MDELE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MDPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS+TFILM)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Dmp Parm"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MDPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MDPLOT2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_MDPLOT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MESH_clnup) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MESH_clnup__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFMCC_Clear) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clear"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFMCC_Clear__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFMCC_Stat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFMCC_Stat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFRUR_tables) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Relaxation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFRUR_tables__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFR_tables) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Relaxation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFR_tables__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFR_values) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Relaxation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFR_values__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFSCC_Clear) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clear"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFSCC_Clear__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFSCC_Stat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFSCC_Stat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFSUR_Converg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFSUR_Converg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFSUR_Relax) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Relaxation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFSUR_Relax__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFSUR_tables) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFSUR_tables__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Addelems) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add elems"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Addelems__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Capture) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Capture"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Capture__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Converg) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Converg__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Define_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Define_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Definefield) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Definefield__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Extern) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "External"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Extern__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_FieldGrp) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Field Grp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_FieldGrp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Freq) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Frequency"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Freq__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Import) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Import"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Import__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_MCCOrder) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Order"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_MCCOrder__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_MCCSetup) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_MCCSetup__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Mapping) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mapping"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Mapping__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_MaxIter) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Iterations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_MaxIter__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Relax) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Relaxation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Relax__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_SCCOrder) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Order"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_SCCOrder__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_SCCSetup) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_SCCSetup__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Surf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Surface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Surf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Surface) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Surface"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Surface__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Time) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Time Ctrl"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Time__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Vol) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Vol__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_Volume) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Volume"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_Volume__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_tables) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_tables__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFS_values) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Convergence"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFS_values__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_AA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_AA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_AA_LoadComm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Load Transfer"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_AA_LoadComm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_AC_ARECV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Load Transfer"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_AC_ARECV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_AC_ASEND) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Load Transfer"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_AC_ASEND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_AC_CUST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Load Transfer"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_AC_CUST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_AC_LoadComm) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Load Transfer"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_AC_LoadComm__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_AC_Mech) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Load Transfer"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_AC_Mech__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_AC_Solu) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Solution Ctrl"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_AC_Solu__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_AC_Ther) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Load Transfer"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_AC_Ther__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_AX) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_AX__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_Clear) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Clear"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_Clear__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_MapSetup) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mapping"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_MapSetup__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_MaxIter) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Iterations"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_MaxIter__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_Single) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_Single__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_Time) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Time Ctrl"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_Time__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MFX_Write) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Write input"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MFX_Write__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MF_ON) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (FULL_ANS&LINPLUS&THERMAL&MULTDISC)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Select method"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MF_ON__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MF_Off) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MF_Off__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Copy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_MGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MGEN2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_MGEN2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MLIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Picked"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_MLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MLIST_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List All"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MLIST_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MMF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((POST1+RESULTS+MAGNETIC)*NOFILTER)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "MMF"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MMF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MODCOM_CQC) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "CQC Method"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MODCOM_CQC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MODCOM_DSUM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "DSUM Method"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MODCOM_DSUM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MODCOM_GRP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "GRP Method"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MODCOM_GRP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MODCOM_NRLSM) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "NRLSUM Method"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MODCOM_NRLSM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MODCOM_SRSS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "SRSS Method"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MODCOM_SRSS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MODCOM_psd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(PSDRANDM*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mode Combine"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MODCOM_psd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MODCYC_0) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Create 2nd Sectr"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MODCYC_0__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MODCYC_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Do Modal Analy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MODCYC_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MODE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list ((pr_elmag+magnetic+electric)*CFD*FLUID*THERMAL*STRUCTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "For Harmonic Ele"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MODE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MODFUL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Gen Full Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MODFUL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MONITOR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (NLCNTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Monitor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_MONITOR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MONITOR_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (NLCNTRL)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS*LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Monitor"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MONITOR_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MORPHING) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Phys Morphing"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MORPHING__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MORPH_ON) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION*NOFILTER)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element Morphing"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MORPH_ON__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MOVE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "To Intersect"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_MOVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MOVE_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MOVE_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MP*VPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Columns of Array Parameters"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MP*VPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPAMOD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna+pr_cfd)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Convert ALPx"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPAMOD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPANEL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY3)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPANEL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPBISO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPBISO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPCHG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 1 [list (LINPLUS*ELECMAG*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Change Mat Num"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPCHG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPDATA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Prop Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPDATA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPDEF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPDEF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Delete Mat Props"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPDRES) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Modify Temps"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPDRES__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPDYN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY7)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlinear MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPDYN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPDYN2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY27)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlinear MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPDYN2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPDYNConcr) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY40)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPDYNConcr__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPDYNFluid) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY39)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlinear MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPDYNFluid__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPEOS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY29*KEY30*KEY31*KEY32)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPEOS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPEOS34) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY34)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPEOS34__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPEOS35) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY35)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPEOS35__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPEOS36) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY36*KEY37)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPEOS36__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPEOS38) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY38)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPEOS38__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPEOS42) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY42)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPEOS42__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPEPLO) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPEPLO__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPFSPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Fatigue Stress"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPFSPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPKEY1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPKEY1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLIN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY1*KEY2*KEY5)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLIN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLIST) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Specified Matl, All Temps"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLIST_all) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLIST_all__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLIST_evlt) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "List"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLIST_evlt__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLIST_temp) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "All Matls, Specified Temp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLIST_temp__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLib_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLib_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLib_List) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "List Material"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLib_List__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLib_NA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLib_NA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLib_NA2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLib_NA2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLib_Read) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Import Library"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLib_Read__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLib_Read1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Import Library"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLib_Read1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLib_UNITS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select Units"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLib_UNITS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLib_UNITS2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select Units"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLib_UNITS2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLib_Write) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Export Library"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLib_Write__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPLib_Write1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Export Library"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPLib_Write1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPMATP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Graph"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPMATP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPNOEP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPNOEP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPOPLFA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Results of Factorial Evaluation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPOPLFA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPOPLGR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Results of Gradient Evaluation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPOPLGR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPOPLSW) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Global Sweep Generation"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPOPLSW__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLDI) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PR_STRUC*pr_set)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Deformed Shape"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLDI__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLES) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Element Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLES__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLET) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Elem Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLET__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLLS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line Elem Res"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLLS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLNS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Solu"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLNS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Graph"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLPA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Path Items"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLPA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLSE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Linearized Strs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLSE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLTR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (POST1+RESULTS+CFD)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (THERMAL*FLOTRAN)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Flow Tra"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLTR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLVAp26) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Graph Variables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLVAp26__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLVE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Predefined"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLVE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPLVE_user) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "User-defined"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPLVE_user__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPPlasHyd) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY41)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPPlasHyd__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPREAD) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Read from File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPREAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTB) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY6*KEY8*KEY17*KEY18*KEY19*KEY21)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTB2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY25*KEY26*KEY28)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTB2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTB2_moon) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY8)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTB2_moon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTB2a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY10*KEY12*KEY13*KEY15*KEY16)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTB2a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTBPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Data Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTBPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTBPLOT_ex1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Data Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTBPLOT_ex1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTBPLOT_ex2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Data Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTBPLOT_ex2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTBPLOT_ex3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Plot Data Tables"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTBPLOT_ex3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTB_moon) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY8)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Mooney-Rivlin Data Calculation Options"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTB_moon__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTBa) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY22*KEY23*KEY24*KEY33)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTBa__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTEMP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Temp Table"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTEMP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTGEN) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Generate Temp"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTGEN__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPTRES) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Restore Temps"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPTRES__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPWRITE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Write to File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPWRITE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MP_ENT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi-Plotting"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MP_ENT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MP_ENT2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MP_ENT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MP_GRF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi-Plotting"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MP_GRF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MP_add) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MP_add__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MP_dele) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MP_dele__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MP_edit) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MP_edit__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MP_poly) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (lsdyna)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Polynomial"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MP_poly__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPfld) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ") Anisotropic FLD"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPfld__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPspring) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY43*KEY44*KEY45*KEY46*KEY47*KEY48*KEY49*KEY50)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define MAT Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPspring__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MPstein) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (KEY51*KEY52*KEY53)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Define Steinberg Material Models"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MPstein__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MSHA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Mesher & Shape"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MSHA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MSHC_a) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area Mesh"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_MSHC_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MSHC_a_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Area Mesh"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MSHC_a_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MSHC_l) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line Mesh"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_MSHC_l__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MSHC_l_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Line Mesh"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MSHC_l_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MULTIPLOT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi-Plot Contrls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MULTIPLOT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MULTIWIND) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "MultiWindow Layout"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MULTIWIND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MW1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi-Plotting"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MW1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MW2A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi-Plotting"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MW2A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MW2B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi-Plotting"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MW2B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MW3A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi-Plotting"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MW3A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MW3B) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi-Plotting"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MW3B__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MW4) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Multi-Plotting"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MW4__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MXPAND) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Expand Modes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MXPAND__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_M_EVAL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Evaluate Const"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_M_EVAL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_M_EVAL2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_M_EVAL2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MacLib) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Execute Data Block"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MacLib__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Macro) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Execute Macro"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Macro__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Mass) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mass"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_Mass__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Mass_RCA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Mass_RCA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Mass_RCB) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Mass_RCB__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Mass_RCC) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Mass_RCC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Mass_RCD) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Mass_RCD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Masss) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Masss__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MatCopyExec) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Copy Material Model"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MatCopyExec__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MeCirBD_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MeCirBD_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MeCirBD_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MeCirBD_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MeROMBD_1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MeROMBD_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MeROMBD_2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MeROMBD_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MeROMBD_3) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MeROMBD_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MeROMB_20) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MeROMB_20__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MeROMB_30) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MeROMB_30__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Menu_Refresh) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Menu_Refresh__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MeshAttrib) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Default Attribs"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MeshAttrib__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Mesh_Check) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)+(igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Model Checking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Mesh_Check__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Mesh_tool) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "MeshTool"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Mesh_tool__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Message) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Message__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Msgctrls) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Message Controls"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Msgctrls__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MshrOpts) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Mesher Opts"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MshrOpts__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_MshrStat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (igesfail)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Meshing"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_MshrStat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_N) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "In Active CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_N__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_N/A_Prod) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_N/A_Prod__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NANG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (mdl2d)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Vectors"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NANG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NANG_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "by Vector"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NANG_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NANG_3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NANG_3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NAXIS) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7+AXISSECT)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "About AXIS Section"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NAXIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NCNV) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+(STATIC*(TRNSIENT+FULL)*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Criteria to Stop"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NCNV__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NDELE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NDELE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NDIS) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "ND distances"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NDIS__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NDREAD) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "NODOUT file"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NDREAD__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NEQIT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Equilibrium Iter"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NEQIT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NFORCE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sum @ Each Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NFORCE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NGEN_copy) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Copy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NGEN_copy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NGEN_copy_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NGEN_copy_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NGEN_move) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Set of Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NGEN_move__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NGEN_move_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NGEN_move_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NKPT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Keypoint"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NKPT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NLDIAG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (pp*LSDYNA)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlinear Diagnostics"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NLDIAG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NLHIST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Results Tracking"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NLHIST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NLIST_SORT) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NLIST_SORT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NLIST_p) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NLIST_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NLOG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Natural Log"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NLOG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NLPOST) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1&RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nonlinear Diagnostics"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NLPOST__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NMODIF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Single Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NMODIF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NMODIF_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NMODIF_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NMODIF_ang) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "By Angles"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NMODIF_ang__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NMODIF_ang2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NMODIF_ang2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NODE2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NODE2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NODE3) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NODE3__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NOORDER) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS*THERMAL*ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reset Elem Order"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NOORDER__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NORA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NORA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NORA_ara2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Areas"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NORA_ara2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NORL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NORL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NORL_line2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Lines"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NORL_line2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NORL_with) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "with Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NORL_with__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NORL_with2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "...with Area"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NORL_with2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NREAD_NRRANG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Read Node File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NREAD_NRRANG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NREF) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NREF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NREF_1) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Refine Mesh at NODE"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NREF_1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NREF_2a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NREF_2a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NREF_2b) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NREF_2b__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NROTAT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "To Active CS"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NROTAT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSCALE_copy) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Scale & Copy"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NSCALE_copy__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSCALE_copy_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSCALE_copy_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSCALE_move) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Scale & Move"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NSCALE_move__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSCALE_move_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSCALE_move_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSEL_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Also select nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NSEL_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSEL_A_resul) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSEL_A_resul__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSEL_R) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Reselect nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NSEL_R__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSEL_R_resul) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSEL_R_resul__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSEL_S) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NSEL_S__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSEL_S_ARC) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Select Node"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSEL_S_ARC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSEL_S_resul) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSEL_S_resul__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSEL_U) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Unselect nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NSEL_U__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSEL_U_resul) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSEL_U_resul__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSEL_results) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSEL_results__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSOL) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSOL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSOL1) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSOL1__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSOL1_a) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSOL1_a__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSOL1e) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSOL1e__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSOL_A) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSOL_A__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSOL_Ae) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSOL_Ae__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSOLe) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST26)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodal Data"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSOLe__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSORT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Sort Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSORT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSORT2) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSORT2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSORT_CP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSORT_CP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSORT_EP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSORT_EP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSORT_LP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSORT_LP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSORT_RP) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Hidden"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSORT_RP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSYM) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NSYM__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NSYM_2) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NSYM_2__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NUMCMP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Compress Numbers"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NUMCMP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NUMEXP_HREXP) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list ((PREP7*SOLUTION)+((TRNSIENT+full)*(HARMONIC+full)*SUBSTRUC*NOFILTER))]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Range of Solu's"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NUMEXP_HREXP__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NUMMRG) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Merge Items"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NUMMRG__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NUMOFF) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Add Num Offset"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NUMOFF__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NUMSTR) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Set Start Number"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NUMSTR__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NUMSTR_defa) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Reset Start Num"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NUMSTR_defa__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NUMSTR_stat) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Start Num Status"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NUMSTR_stat__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NUSORT) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (POST1+RESULTS)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Unsort Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NUSORT__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NWPA) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NWPA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NWPL) { top {key 0} {level 0} } {
   global env
   incr level
   set branchLeafs [list]
   set str [::msgcat::mc "Nodes"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_NWPL__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NWRITE) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Write Node File"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NWRITE__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_N_p) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   set branchLeafs [list]
   set str [::msgcat::mc "On Working Plane"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon pickicon
   set branchName [list Fnc_N_p__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_Nd_Surf) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (FULL_ANS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Node to Surf"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_Nd_Surf__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_NonlinRC) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 1 [list (ELECMAG)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc ""]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_NonlinRC__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
proc ::euidl::uidlmenu::uidlmenuItems::uidlmenu(Fnc_OMEGA) { top {key 0} {level 0} } {
   global env
   incr level
   if {![ans_getfilter 0 [list (PREP7*SOLUTION)]]} {
      if {$AnsysGUI::MainMenuArr(pref,filtering)} {
         lappend tags disabled
      } else {
         return -code error
      }
   }
   if {![ans_getfilter 1 [list (LINPLUS)]]} {
      return -code error
   }
   set branchLeafs [list]
   set str [::msgcat::mc "Global"]
   lappend tags leaf
   lappend tags leaflevel__$level
   set icon dlgicon
   set branchName [list Fnc_OMEGA__$top "$str" $tags $icon]
   if { $key == 1 } {
      return $branchName
   }
   if { [string match {} $branchLeafs] } {
      return -code error
   }
   return $branchLeafs
}
